var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"Home","text":""},{"location":"index.html#about","title":"About","text":"<p>Kaptive is a system for surface polysaccharide typing from bacterial genome sequences. It consists of two main components:</p> <ol> <li>Curated reference databases of     surface polysaccharide gene clusters (loci).</li> <li>A command-line interface (CLI) with three modes:<ul> <li>assembly: surface polysaccharide typing from assemblies</li> <li>extract: extract features from Kaptive databases in different   formats</li> <li>convert: convert Kaptive results to different formats</li> </ul> </li> </ol> <p>Kaptive can be found:</p> <ul> <li>On GitHub alongside the   reference databases.</li> <li>On PathogenWatch where it is used to   serotype Klebsiella and Acinetobacter baumannii isolates.</li> <li>As a GUI in Kaptive Web   (source code), which   includes a third-party   database   for Vibrio parahaemolyticus<sup>1</sup>.</li> </ul> <p>For full documentation, including install and usage instructions, click here.</p>"},{"location":"index.html#citation","title":"Citation","text":"<ul> <li>If you use Kaptive in your research, please cite<sup>2</sup>.</li> <li>If you use the Klebsiella K or O locus databases, please cite<sup>3</sup>   and<sup>4</sup>.</li> <li>If you use Kaptive Web, please   cite<sup>5</sup>.</li> <li>If you use the A. baumannii K or OC locus database(s) in your   research please cite<sup>6</sup> and<sup>7</sup>.</li> </ul>"},{"location":"index.html#tutorial","title":"Tutorial","text":"<p>Step-by-step video and documented tutorials are available, covering:</p> <ul> <li>Kaptive's features and their scientific rationale</li> <li>How to run Kaptive</li> <li>Examples, illustrating how to run and interpret results</li> <li>Further investigations (e.g. exploring novel loci, IS insertions)</li> </ul> <p>Note</p> <p>The tutorials are based on Kaptive 2.0, but the principles are similar for Kaptive 3.0.</p>"},{"location":"index.html#people","title":"People","text":"<ul> <li>Dr. Tom Stanton</li> <li>A/Prof. Kelly L. Wyres</li> <li>Prof. Kathryn E. Holt</li> <li>Dr. Ryan R. Wick</li> <li>A/Prof. Johanna   Kenyon</li> </ul> <p>Contact Kelly and Tom for help with Kaptive, or to report bugs or request features.</p>"},{"location":"index.html#references","title":"References","text":"<ol> <li> <p>Van Der Graaf-van Bloois, L., Chen, H., Wagenaar, J.A., Zomer, A.L., 2023. Development of Kaptive databases for Vibrio parahaemolyticus O- and K-antigen genotyping. Microbial Genomics 9. https://doi.org/10.1099/mgen.0.001007 \u21a9</p> </li> <li> <p>Stanton TD, Hetland MAK, L\u00f6hr IH, Holt KE, Wyres KL. Fast and Accurate in silico Antigen Typing with Kaptive 3. 2025 Microbial Genomics 11(6):001428. https://doi.org/10.1099/mgen.0.001428 \u21a9</p> </li> <li> <p>Wyres, K.L., Wick, R.R., Gorrie, C., Jenney, A., Follador, R., Thomson, N.R., Holt, K.E., 2016. Identification of Klebsiella capsule synthesis loci from whole genome data. Microbial Genomics 2, e000102. https://doi.org/10.1099/mgen.0.000102 \u21a9</p> </li> <li> <p>Lam, M.M.C., Wick, R.R., Judd, L.M., Holt, K.E., Wyres, K.L., 2022. Kaptive 2.0: updated capsule and lipopolysaccharide locus typing for the Klebsiella pneumoniae species complex. Microbial Genomics 8. https://doi.org/10.1099/mgen.0.000800 \u21a9</p> </li> <li> <p>Wick, R.R., Heinz, E., Holt, K.E., Wyres, K.L., 2018. Kaptive Web: User-Friendly Capsule and Lipopolysaccharide Serotype Prediction for Klebsiella Genomes. Journal of Clinical Microbiology 56, 10.1128/jcm.00197-18. https://doi.org/10.1128/jcm.00197-18 \u21a9</p> </li> <li> <p>Wyres, K.L., Cahill, S.M., Holt, K.E., Hall, R.M., Kenyon, J.J., 2020. Identification of Acinetobacter baumannii loci for capsular polysaccharide (KL) and lipooligosaccharide outer core (OCL) synthesis in genome assemblies using curated reference databases compatible with Kaptive. Microbial Genomics 6. https://doi.org/10.1099/mgen.0.000339 \u21a9</p> </li> <li> <p>Cahill, S.M., Hall, R.M., Kenyon, J.J., 2022. An update to the database for Acinetobacter baumannii capsular polysaccharide locus typing extends the extensive and diverse repertoire of genes found at and outside the K locus. Microbial Genomics 8. https://doi.org/10.1099/mgen.0.000878 \u21a9</p> </li> </ol>"},{"location":"Databases.html","title":"Databases","text":""},{"location":"Databases.html#what-is-a-locus","title":"What is a locus?","text":"<p>A locus in the Kaptive sense refers to a biosynthetic gene cluster that is responsible for the synthesis of a bacterial surface polysaccharide, e.g. the Klebsiella pneumoniae K locus is responsible for the synthesis of the capsular polysaccharide, also known as the K antigen. Each locus in the Kaptive databases has been defined based on a unique set of genes, with the assumption that this encodes a unique polysaccharide structure. In many cases, these unique structures will result in unique immunological serotypes.</p> <p>The gene translations (protein sequences) from each locus are compared by pairwise alignment, and must fall under a defined percent identity threshold to be considered 'unique'. Some genes (such as the core assembly machinery) will be highly similar, however the genes responsible for the polysaccharide structural diversity are expected to be more variable. The specific identity thresholds vary across species. The thresholds corresponding to the databases distributed with Kaptive are as follows:</p> Species Pairwise protein identity threshold Klebsiella pneumoniae 82.5% Acinetobacter baumannii 85%"},{"location":"Databases.html#format","title":"Format","text":""},{"location":"Databases.html#genbank-file","title":"Genbank file","text":"<p>Kaptive stores databases in Genbank format consisting of unique loci each with a single record with the following requirements:</p> <ul> <li>The <code>source</code> feature must contain a <code>note</code> qualifier which begins with   a label such as <code>K locus:</code>. Whatever follows is used as the locus name   reported in the Kaptive output. The label is automatically determined,   and any consistent label ending in a colon will work. However, the   user can specify exactly which label to use with <code>--locus_label</code>, if   desired.</li> <li>The <code>source</code> feature may optionally contain a <code>note</code> qualifier which   begins with a label such as <code>K type:</code> that specifies the   phenotype (e.g. serotype or defined polysaccharide structure name) associated with the locus (if known). In cases where only   some loci are associated with known phenotypes we recommend adding a   <code>note</code> such as <code>K type: unknown</code>. If no <code>type</code> notes are specified for   any loci, Kaptive will list them as <code>unknown</code> in the output.   (Kaptive v2.0+)</li> <li>Any locus gene should be annotated as <code>CDS</code> features. All <code>CDS</code>   features will be used and any other type of feature will be ignored.</li> <li>If the gene has a name, it should be specified in a <code>gene</code> qualifier.   This is not required for Kaptive to run, but if absent the gene will   only be named using its numbered position in the locus and it will not   be checked for any specific sequence variations relevant to phenotype   prediction.</li> </ul> <p>Example piece of input Genbank file:</p> <pre><code>source          1..23877\n                /organism=\"Klebsiella pneumoniae\"\n                /mol_type=\"genomic DNA\"\n                /note=\"K locus: KL1\"\n                /note=\"K type: K1\"\nCDS             1..897\n                /gene=\"galF\"\n</code></pre>"},{"location":"Databases.html#nomenclature","title":"Nomenclature","text":"<p>In constructing the databases included with Kaptive, we have used the following nomenclature rules:</p> <ul> <li>Loci are named after their respective antigen (K, O, or   OC) followed by the letter L (which stands for Locus),   which separates the label for the genotype from the phenotype (e.g.   KL1 -&gt; K1). These letters should be in upper case.</li> <li>Loci are numbered, first, by their corresponding phenotype, and second,   in the order in which they were discovered. For example, Klebsiella   K-loci KL1-KL72, KL74 and KL79-KL82 correspond to the originally defined K-types K1-K72, K74 and K79-K82, respectively. K-loci 101 and greater   correspond to K-loci for which the phenotypes were unknown at the time of locus discovery, numbered in the order in which they   were discovered.</li> <li>Locus genes are named in three parts delimited by an underscore   (_):</li> <li>The locus the gene belongs to, e.g. <code>KL1_</code> for a gene in the <code>KL1</code>       locus.</li> <li>The position of the gene in the locus, e.g. <code>KL1_01</code> for the first       gene in the <code>KL1</code> locus.</li> <li>The name of the gene as a three-letter italicized symbol written       in lower case letters and usually suffixed with an italicized       capital letter, e.g. <code>KL1_01_galF</code> for the galF gene in the       <code>KL1</code> locus. If the gene name is unknown, this part will be blank       and the gene instead would be called <code>KL1_01</code>.</li> </ul> <p>Note</p> <p>Databases must follow this nomenclature system for distribution within Kaptive.</p> <p></p>"},{"location":"Databases.html#phenotype-logic","title":"Phenotype logic","text":"<p>Phenotype logic (previously called \"special logic\") is a set of rules that Kaptive uses to predict the polysaccharide phenotype based on the genes it finds. This was initially implemented for the Klebsiella pneumoniae O locus, whereby additional genes outside of the locus are used to predict the O antigen (sub)type. This logic was extended to the A. baumannii K locus in Kaptive v2.0.2.</p> <p>In Kaptive 3, we thought about how we could extend this given what we know about truncations or other sequence variations of specific genes in the locus and the impact on the phenotype. For example, in the Klebsiella pneumoniae K locus, we know that a truncation of the core initiating glycosyltransferase (wcaJ) results in a capsule-null phenotype.</p> <p>The relevant sequence variations are detailed in the database logic files, each labelled with the same file prefix as its respective locus database, and marked with the extension <code>.logic</code>. Each line consists of three tab-separated columns and represents a phenotype rule:</p> <ol> <li>loci - the loci the rule applies to (or ALL if the rule     applies to all loci in the database)</li> <li>genes - the genes (and optional state) the rule applies to (or     ALL if the rule applies to all genes in the locus)</li> <li>phenotype - the resulting phenotype that appears in the     Type column of the Kaptive tabular     output, replacing the default phenotype i.e. the one specified in     the locus genbank source identifier in the matching locus database.</li> </ol> <p>Let's look at an example of a logic file for the K. pneumoniae K locus:</p> loci genes phenotype ALL wcaJ1,truncated Capsule null KL22 KL22_17,truncated K37 <p>In the first line, you can see that if wcaJ1 is truncated in any locus (selected with ALL), the phenotype will be predicted as 'Capsule null'. Here, any gene with the name wcaJ1 will be considered, and the state of the gene is specified as truncated. In the last line, you can see that if KL22_17 (acetyl-transferase) is truncated in locus KL22, the phenotype is predicted as 'K37', the non-acetylated version of the K22 capsule.</p> <p>Note</p> <p>The gene name and state are delimited by a comma.</p> <p>Note</p> <p>The default phenotype is the \"type\" label in the Genbank record (e.g. K1).</p> <p>Let's look at an example that uses extra genes outside of the locus (from the K. pneumoniae O locus database):</p> loci genes phenotype OL2\u03b1.1;OL2\u03b1.2;OL2\u03b1.3 orf8 O2\u03b1\u03b3 <p>Here, the first line states that if orf8 is present in a genome carrying any of the OL2\u03b1.1, OL2\u03b1.2 or OL2\u03b1.3 loci, the phenotype will be predicted as 'O2\u03b1\u03b3'.</p> <p>Note</p> <p>Each specific locus and gene is delimited by a semicolon.</p> <p>Note</p> <p>Default state is 'presence'.</p> <p>This logic is applied during the phenotype prediction step of typing and is reported in the Type column of the Kaptive tabular output.</p> <p></p>"},{"location":"Databases.html#databases-distributed-with-kaptive","title":"Databases distributed with Kaptive","text":"<p>Kaptive is distributed with databases for detection of Klebsiella pneumoniae Species Complex (KpSC) and Acinetobacter baumannii surface antigen synthesis loci in the data directory, (see details below). You can also generate your own databases for use with Kaptive by following these guidelines.</p> <p>The existing databases were developed and curated by Kelly Wyres (Klebsiella) and Johanna Kenyon (A. baumannii).</p> <p>A third-party Kaptive database is available for Vibrio parahaemolyticus K and O loci, created by Aldert Zomer and team (see here). The database can be downloaded and used as input to command-line Kaptive, it is also available in the online tool Kaptive-Web along with our Klebsiella and A. baumannii databases.</p> <p>We are always keen to expand the utility of Kaptive for the research community, so if you have created a database that you feel will be useful for others and you are willing to share this resource, please get in touch via the issues page or email.</p> <p>Similarly, if you have identified new locus variants not currently in the existing databases, please let us know!</p>"},{"location":"Databases.html#klebsiella-pneumoniae-species-complex-kpsc-k-locus-databases","title":"Klebsiella pneumoniae Species Complex (KpSC) K locus databases","text":"<p>The KpSC K locus reference database (<code>Klebsiella_k_locus_primary_reference.gbk</code>) comprises full-length (galF to ugd) annotated sequences for each distinct Klebsiella K locus, where available:</p> <ul> <li>K-loci KL1-KL72, KL74 and KL79-KL82 correspond to the originally defined K-types K1-K72, K74 and K79-K82, respectively. K-loci 101 and greater   correspond to K-loci for which the phenotypes were unknown at the time of locus discovery, numbered in the order in which they   were discovered. </li> <li>KL101 and above are defined from DNA sequence data on the basis of   gene content, for which no matched phenotypes were known at the time. However, the polysaccharide structures and/or serotypes corresponding to several of these loci have since been described e.g. serotypes K102, K112, K122, K136 and K149).</li> </ul> <p>Note</p> <p>Insertion sequences (IS) are excluded from this database since we assume that the ancestral sequence was likely IS-free and IS transposase genes are not specific to the K locus.</p> <p>Synthetic IS-free K locus sequences were generated for K loci for which no naturally occurring IS-free variants have been identified to date.</p> <p>Note</p> <p>KL156-D1 is included in the primary reference database since no full-length version of this locus has been identified to date.</p> <p>Database versions:</p> <ul> <li>Kaptive releases v0.5.1 and below include the original Klebsiella K   locus databases, as described in Wyres, K. et al. Microbial Genomics   2016.</li> <li>Kaptive v0.6.0 and above include four novel primary Klebsiella K   locus references defined on the basis of gene content (KL162-KL165) in   Wyres et al. Genome Medicine   2020.</li> <li>Kaptive v0.7.1 and above contain updated versions of the KL53 and   KL126 loci (see table below for details). The updated KL126 locus   sequence is described in McDougall, F. et al. Research in   Microbiology 2021.</li> <li>Kaptive v0.7.2 and above include a novel primary Klebsiella K locus   reference defined on the basis of gene content (KL166), described in   Le, MN. et al. Microbial Genomics   2022.</li> <li>Kaptive v0.7.3 and above include four novel primary Klebsiella K   locus references defined on the basis of gene content (KL167-KL170),   described in Gorrie, C. et al. Nature Communications   2022.</li> <li>Kaptive v2.0 and above include 16 novel primary Klebsiella K locus   references defined on the basis of gene content (KL171-KL186) and   described in Lam, M.M.C et al. Microbial Genomics   2022.</li> <li>Kaptive v3.2 and above introduced a re-annotation of Klebsiella K   locus reference genes curated by Dr. Tom Stanton and A/Prof Johanna   Kenyon. All K-locus genes where throughly screened against curated   annotations with a variety of homology detection methods to provide a   more accurate functional description and standardised gene nomenclature.</li> </ul> <p>Changes to the Klebsiella K locus primary reference database:</p> Locus Change Reason Date of change Kaptive version no. KL53 Annotation update: wcaJ changed to wbaP Error in original annotation 21 July 2020 v 0.7.1 KL126 Sequence update: new sequence from isolate FF923 includes rmlBADC genes between gnd and ugd Assembly scaffolding error in original sequence from isolate A-003-I-a-1 21 July 2020 v 0.7.1 KL37 Removed from the database Locus is a deletion (atr) variant of KL22 22 March 2024 v 3.0.0 All Updated gene names and functional annotations Database standardisation March 2026 v3.2.0"},{"location":"Databases.html#klebsiella-o-locus-database","title":"Klebsiella O locus database","text":"<p>In Kaptive 3.1.0, we introduced new O-antigen nomenclature in the Klebsiella O locus database (<code>Klebsiella_o_locus_primary_reference.gbk</code>) along wth the publication of this review: O-antigen polysaccharides in Klebsiella pneumoniae: structures and molecular basis for antigenic diversity.</p> <p>We have also summarised the O-antigen nomenclature update on the Wyres Lab website.</p> <p>The Klebsiella O locus database (<code>Klebsiella_o_locus_primary_reference.gbk</code>) contains annotated sequences for 13 distinct Klebsiella O loci.</p> <p>O locus classification requires some special logic, as the O1 and O2 serotypes are associated with the same loci and the distinction between O1 and each of the defined O2 subtypes (2\u03b1, 2\u03b2, 2\u03b3) is determined by the presence/absence of 'extra genes' (gml2\u03b2 and orf8) elsewhere in the chromosome as indicated in the table below. Kaptive therefore looks for these genes to predict antigen (sub)types.</p> <p>Note</p> <p>You can find information about the Klebsiella O locus database in Kaptive versions &lt;3.1.0 here.</p> New serotype designation Required genes/loci (implemented in Kaptive v.3.1+) Prior Kaptive designation (v.2.0.8\u2013v.3.0.0b6) Prior Kaptive genes/loci (v.2.0.8\u2013v.3.0.0b6) O1\u03b1\u03b2,2\u03b1 OL2\u03b1.(1/2/3), wbbYZ O1ab O1/O2v1, wbbYZ O1\u03b1,2\u03b1 OL2\u03b1.(1/2/3), wbbY O1a O1/O2v1, wbbY O1\u03b1\u03b2,2\u03b2 OL2\u03b1.(1/2/3), gml2\u03b2, wbbYZ O1ab O1/O2v2, wbbYZ O1\u03b1,2\u03b2 OL2\u03b1.(1/2/3), gml2\u03b2, wbbY O1a O1/O2v2, wbbY O1\u03b1\u03b2,2\u03b3 OL2\u03b1.(1/2/3), orf8, wbbYZ O1ab O1/O2v3, wbbYZ O2\u03b1 OL2\u03b1.(1/2/3) O2a O1/O2v1 O2\u03b2 OL2\u03b1.(1/2/3), gml2\u03b2 O2afg O1/O2v2 O2\u03b1\u03b3 OL2\u03b1.(1/2/3), orf8 O2a O1/O2v3 O3\u03b1 + O3\u03b2 OL3\u03b1/\u03b2 O3/O3a O3/O3a O3\u03b3 OL3\u03b3 O3b O3b O4 OL4 O4 O4 O5 OL5 O5 O5 O10 OL10 OL103 OL103 O11\u03b1\u03b2,2\u03b1 OL2\u03b1.(1/2/3), wbmVWX O2ac O1/O2v1, wbmVWX O11\u03b1,2\u03b1 OL2\u03b1.(1/2/3), wbmVW O2ac O1/O2v1, wbmVW O11\u03b1\u03b2,2\u03b2 OL2\u03b1.(1/2/3), gml2\u03b2, wbmVWX O2ac O1/O2v2, wbmVWX O11\u03b1,2\u03b2 OL2\u03b1.(1/2/3), gml2\u03b2, wbmVW O2ac O1/O2v2, wbmVW O11\u03b1\u03b2,2\u03b3 OL2\u03b1.(1/2/3), orf8, wbmVWX O2ac O1/O2v3, wbmVW O12 OL12 O12 O12 O13 OL13 O13 OL13 O14 OL14 OL102 OL102 O15 OL15 OL104 OL104"},{"location":"Databases.html#acinetobacter-baunannii-k-and-oc-locus-databases","title":"Acinetobacter baunannii K and OC locus databases","text":"<p>The A. baumannii K (capsule) locus reference database (<code>Acinetobacter_baumannii_k_locus_primary_reference.gbk</code>) contains annotated sequences for 241 distinct K loci.</p> <p>The A. baumannii OC (lipooligosaccharide outer core) locus reference database (<code>Acinetobacter_baumannii_OC_locus_primary_reference.gbk</code>) contains annotated sequences for 22 distinct OC loci.</p> <p>Warning</p> <p>These databases have been developed and tested specifically for A. baumannii and may not be suitable for screening other Acinetobacter species. You can check that your assembly is a true A. baumannii by screening for the oxaAB gene e.g. using blastn.</p> <p>Database versions:</p> <ul> <li>Kaptive v0.7.0 and above include the original A. baumannii K and OC   locus databases, as described in Wyres, KL. et al. Microbial Genomics   2020.</li> <li>Kaptive v2.0.1 and above include 149 novel primary A. baumannii K   locus references as described in Cahill, S.M. et al. 2022. An update   to the database for Acinetobacter baumannii capsular polysaccharide   locus typing extends the extensive and diverse repertoire of genes   found at and outside the K locus. Microbial   Genomics.</li> <li>Kaptive v2.0.2 and above include special logic parameters that enable   prediction of the capsule polysaccharide type based on KL or the   detected combination of a specific KL with 'extra genes' elsewhere in   the chromosome as indicated in the table below and described in   Cahill, S.M. et al. 2022. An update to the database for A. baumannii   capsular polysaccharide locus typing extends the extensive and diverse   repertoire of genes found at and outside the K locus. Microbial   Genomics.</li> <li>Kaptive v2.0.5 and above includes a further 10 A. baumannii OC locus   references (OCL13-OCL22) as described in Sorbello, B. et al.   Identification of further variation at the lipooligosaccharide outer   core locus in Acinetobacter baumannii genomes and extension of the   OCL reference sequence database for Kaptive. Microbial Genomics.</li> </ul> <p></p>"},{"location":"Databases.html#database-keywords","title":"Database keywords","text":"<p>When Kaptive is installed, it may be difficult to find the databases in the file system. However, each <code>&lt;database&gt;</code> argument in the Kaptive CLI accepts either a path to a Genbank file or a keyword that refers to a database distributed with Kaptive. The keywords are listed below.</p> Database Keywords Klebsiella pneumoniae K locus primary reference database <ul> <li>kpsc_k</li> </ul> Klebsiella pneumoniae O locus primary reference database <ul> <li>kpsc_o</li> </ul> Acinetobacter baumannii K locus primary reference database <ul> <li>ab_k</li> </ul> Acinetobacter baumannii OC locus primary reference database <ul> <li>ab_o</li> </ul>"},{"location":"Databases.html#extract","title":"Extract","text":"<p>Kaptive 3.0.0 and above includes a new command-line mode <code>extract</code> that allows you to extract features from a Kaptive database in the following formats:</p> <ul> <li>fna: Locus nucleotide sequences in fasta format.</li> <li>ffn: Gene nucleotide sequences in fasta format.</li> <li>faa: Protein sequences in fasta format.</li> </ul> <p>See here for usage instructions.</p>"},{"location":"FAQs.html","title":"FAQs","text":""},{"location":"FAQs.html#why-do-i-have-a-large-number-of-klebsiella-genomes-with-fragmented-k-loci","title":"Why do I have a large number of Klebsiella genomes with fragmented K loci?","text":"<p>Unfortunately, the Klebsiella K-locus is a particularly difficult region of the genome to sequence due to its low GC content compared to the rest of the chromosome, which can result in assembly fragmentation. This is particularly evident for draft genomes assembled from Illumina data generated with the Nextera XT library prep kit. These read sets may have little or no read coverage of parts of the K-locus, meaning it is impossible to fully assemble it. Unlike prior versions of Kaptive, Kaptive 3 selects the <code>Best match locus</code> based on gene searches rather than full length locus matches. It requires a match to only 50% of the gene to count it for locus assignment.</p> <p></p>"},{"location":"FAQs.html#why-does-the-klebsiella-k-locus-region-of-my-sample-contain-a-ugd-gene-matching-another-locus","title":"Why does the Klebsiella K-locus region of my sample contain a ugd gene matching another locus?","text":"<p>A small number of the original Klebsiella K locus references are truncated, containing only a partial ugd sequence. The reference annotations for these loci do not include ugd, so are not identified by the 'tblastn' search. Instead \\&lt;b&gt;Kaptive\\&lt;/b&gt; reports the closest match to the partial sequence (if it exceeds the 90% coverage threshold).</p>"},{"location":"FAQs.html#why-are-there-locus-genes-found-outside-the-locus","title":"Why are there locus genes found outside the locus?","text":"<p>For Klebsiella K loci in particular, a number of the K-locus genes are orthologous to genes outside of the K-locus region of the genome. E.g the Klebsiella K-locus man and rml genes have orthologues in the LPS (lipopolysacharide) locus; so it is not unusual to find a small number of genes \"outside\" the locus.</p> <p>However, if you have a large number of genes (&gt;5) outside the locus it may mean that there is a problem with the locus match, or that your assembly is very fragmented or contaminated (contains more than one sample).</p>"},{"location":"FAQs.html#why-has-the-best-matching-locus-changed-after-i-reran-my-analysis-with-kaptive-3","title":"Why has the best matching locus changed after I reran my analysis with Kaptive 3?","text":"<p>Kpative 3 uses a gene wise search to select the best matching locus, whereas Kaptive 2 uses a full length locus search. While the full length locus search was highly accurate for matching loci found in one or a small number of pieces in the query assembly, it was prone to errors when typing more fragmented loci (although these were generally marked with <code>Low</code> and <code>None</code> confidence and recommended to be excluded). The new algorithm implemented in Kaptive 3 is much more sensitive for typing fragmented loci and results in fewer incorrect assignments in our testing.</p>"},{"location":"Installation.html","title":"Installation","text":""},{"location":"Installation.html#dependencies","title":"Dependencies","text":"<p>Kaptive requires the following software and libraries, if installing from source they will need to be installed and available in your path:</p> <ul> <li>Python &gt;=3.9</li> <li>Biopython &gt;=1.83</li> <li>minimap2</li> </ul>"},{"location":"Installation.html#download-and-install-kaptive","title":"Download and install Kaptive","text":"<p>From PyPI:</p> pip install kaptiveInstalled! <p>From Bioconda:</p> conda install -c bioconda kaptiveInstalled! <p>From source:</p> pip install git+https://github.com/klebgenomics/Kaptive.gitInstalled!"},{"location":"Interpreting-the-results.html","title":"Interpreting the results","text":"<p>The four most important columns in the Kaptive tabular output:</p> <ul> <li><code>Best match locus</code> indicating the locus that is best supported by the   available sequence data</li> <li><code>Best match type</code> indicating the predicted phenotype based on that   associated with the <code>Best match locus</code> and taking into account any   special phenotype logic e.g. where a known essential gene is   truncated, Kaptive will report the <code>Best match type</code> as 'Capsule   null'.</li> <li><code>Match confidence</code> indicates a qualitative level of confidence that   the reported <code>Best match locus</code> is correct (see below).</li> <li><code>Problems</code> indicates the features of the assembly locus that may   impact <code>Match confidence</code></li> </ul> <p>Note</p> <p>The <code>Match confidence</code> relates to the locus match, and is not a direct indication of confidence in the <code>Best match type</code>.</p> <p></p>"},{"location":"Interpreting-the-results.html#confidence-score","title":"Confidence score","text":"<p>Kaptive will indicate the best matching locus and its confidence in the locus match. The critera mentioned below can be tweaked using the confidence options.</p>"},{"location":"Interpreting-the-results.html#typeable-loci","title":"Typeable loci","text":"<p>The locus was found in a single piece in the query assembly with no genes below the minimum translated identity according to the locus thresholds and:</p> <ul> <li>no missing genes</li> <li>no (non-truncated) unexpected genes (genes from other loci) inside the   locus region of the assembly</li> </ul> <p>OR</p> <p>The locus was found in more than one piece in the query assembly with no genes below the minimum translated identity according to the locus thresholds and:</p> <ul> <li>no less than N% missing genes (default: 50%)</li> <li>no more than N unexpected gene (genes from other loci) inside the   locus region of the assembly (default: 1)</li> </ul> <p>These criteria were designed in consideration of the locus definition rules (i.e. that each locus represents a unique set of genes defined at a given minimum translated identity threshold) and following systematic analysis of Kaptive outputs for draft genome assemblies compared against manually confirmed loci determined from matched completed genomes.</p> <p>We allow some flexibility with regards to missing genes or additional genes found within the locus when this region of the query assembly is fragmented, because it can be difficult to distinguish genuine from spurious matches for fragmented genes. Fragmentation is common among K. pneumoniae K loci, particularly when the genomes were sequenced using the Illumina technology with the Illumina XT library prep (see FAQs for more details).</p>"},{"location":"Interpreting-the-results.html#untypeable-loci","title":"Untypeable loci","text":"<p>These are loci that do not meet the above criteria. We recommend that users do not accept these results unless they are able to perform manual exploration of the data.</p>"},{"location":"Interpreting-the-results.html#problems","title":"Problems","text":"<ul> <li><code>?</code> = the match was in a multiple pieces, possibly due to a poor match   or discontiguous assembly. The number of pieces is indicated by the   integer directly following the <code>?</code> symbol).</li> <li><code>-</code> = genes expected in the locus were not found.</li> <li><code>+</code> = extra genes were found in the locus.</li> <li><code>*</code> = one or more expected genes was found but with translated   identity below the minimum threshold.</li> <li><code>!</code> = one or more genes was found but truncated.</li> </ul>"},{"location":"Interpreting-the-results.html#exploring-the-other-columns","title":"Exploring the other columns","text":"<p>Many users will not want or need to look beyond the columns described above. However, the rest of the Kaptive output may be useful for those wishing to investigate loci marked with <code>Problems</code> or explore locus variation in more detail. Interesting features include:</p> <ul> <li>Missing genes may indicate a novel locus or deletion variant is   present in the assembly</li> <li>Length discrepancies can indicate a novel locus or deletion variant is   present in the assembly. For Klebsiella K loci, positive length   discrepancies of &gt;700bp often indicate insertion sequence insertions   resulting in so called 'IS variants' of the locus.</li> <li>Other genes inside the locus may indicate a novel locus (with some   exceptions, see the FAQs)</li> <li>Truncated genes may have an impact on the resultant phenotype. Kaptive   will consider truncations when reporting predicting phenotypes, but it   currently considers only gene truncations for which there is good   supporting evidence in the literature, and such evidence is very   limited.</li> </ul> <p>See the tutorials for our tips on investigating loci in more detail outside of Kaptive.</p>"},{"location":"Legacy.html","title":"Legacy Database Information","text":"<p>Note</p> <p>This page contains information about legacy Kaptive databases for backwards compatibility and interpretation of old results.</p> <p></p>"},{"location":"Legacy.html#legacy-klebsiella-pneumoniae-species-complex-o-locus-database","title":"Legacy Klebsiella pneumoniae Species Complex O locus database","text":"<p>The Klebsiella O locus database (<code>Klebsiella_o_locus_primary_reference.gbk</code>) contains annotated sequences for 13 distinct Klebsiella O loci.</p> <p>O locus classification requires some special logic, as the O1 and O2 serotypes are associated with the same loci and the distinction between O1 and each of the four defined O2 subtypes (O2a, O2afg, O2ac, O2aeh) is determined by the presence/absence of 'extra genes' elsewhere in the chromosome as indicated in the table below. Kaptive therefore looks for these genes to predict antigen (sub)types. (Note that the original implementation of O locus typing in Kaptive (\\&lt; v2.0) distinguished O1 and O2 but not the O2 subtypes.)</p> <p>Read more about the O locus and its classification here: The diversity of Klebsiella pneumoniae surface polysaccharides.</p> <p>Find out about the genetic determinants of O1 and O2 (sub)types here: Molecular basis for the structural diversity in serogroup O2-antigen polysaccharides in Klebsiella pneumoniae.</p> <p>Find out about the O1 glycoforms and their genetic determinants here: Identification of a second glycoform of the clinically prevalent O1 antigen from Klebsiella pneumoniae.</p> <p>Database versions:</p> <ul> <li>Kaptive v0.4.0 and above include the original version of the   Klebsiella O locus database, as described in Wick, R. et al. J Clin   Microbiol 2019.</li> <li>Kaptive v2.0 and above include a novel O locus reference (O1/O2v3) and   updated 'Extra genes' for prediction of O1 and O2 antigen (sub)types,   as shown in the table below and described in Lam, M.M.C et al. 2021.   Microbial Genomics 2022.</li> <li>Kaptive v2.0.8 and above include:   1)  updated 'Extra genes' logic for prediction of O1 glycoforms,       reported as O1a (isolate predicted to produce O1a only) and O1ab       (isolate predicted to be able to produce both O1a and O1b       glycoforms);   2)  OL101 re-assigned as OL13 and its associated phenotype prediction       updated to O13, to reflect the description of the novel O13       polysaccharide       structure.</li> </ul> <p>Genetic determinants of O1 and O2 outer LPS antigens as reported in Kaptive:</p> O locus Extra genes Kaptive \\&lt; v2.0 (locus<sup>a</sup>) Kaptive v2.0+ (locus<sup>a</sup>) Kaptive v2.0 - v2.0.7 (type<sup>b</sup>) Kaptive v2.0.8+ (type<sup>b</sup>) O1/O2v1 none O2v1 O1/O2v1 O2a O2a O1/O2v2 none O2v2 O1/O2v2 O2afg O2afg O1/O2v3 none Na O1/O2v3 O2a O2a O1/O2v1 wbbYZ O1v1 O1/O2v1 Na O1ab O1/O2v2 wbbYZ O1v2 O1/O2v2 Na O1ab O1/O2v3 wbbYZ. Na O1/O2v3 Na O1ab O1/O2v1 wbbY only O1v1 O1/O2v1 O1 O1a O1/O2v2 wbbY only O1v2 O1/O2v2 O1 O1a O1/O2v3 wbbY only Na O1/O2v3 O1 O1a O1/O2v1 wbbY OR wbbZ O1/O2v1 Na Na Na O1/O2v2 wbbY OR wbbZ O1/O2v2 Na Na Na O1/O2v3 wbbY OR wbbZ Na Na Na Na O1/O2v1 wbmVW Na O1/O2v1 O2ac O2ac O1/O2v2 wbmVW Na O1/O2v2 O2ac O2ac O1/O2v3 wbmVW Na O1/O2v3 O2ac O2ac O1/O2v1 gmlABD Na O1/O2v1 O2aeh O2aeh O1/O2v2 gmlABD Na O1/O2v2 O2aeh O2aeh O1/O2v3 gmlABD Na O1/O2v3 O2aeh O2aeh O1/O2v1 wbbY AND wbmVW Na O1/O2v1 O1 (O2ac)<sup>b</sup> O1 (O2ac)<sup>b</sup> O1/O2v2 wbbY AND wbmVW Na O1/O2v2 O1 (O2ac)<sup>b</sup> O1 (O2ac)<sup>b</sup> O1/O2v3 wbbY AND wbmVW Na O1/O2v3 O1 (O2ac)<sup>b</sup> O1 (O2ac)<sup>b</sup> <ul> <li><sup>a</sup> as reported in the \u2018Best match locus\u2019 column in the   Kaptive output.</li> <li><sup>b</sup> predicted antigenic serotype reported in the 'Best match   type' column in the Kaptive output (v2.0 and above).</li> <li>Na- not applicable</li> </ul>"},{"location":"Method.html","title":"Method","text":""},{"location":"Method.html#kaptive-assembly","title":"kaptive assembly","text":"<p>For each input assembly, Kaptive runs the <code>kaptive.assembly.typing_pipeline</code> which does the following:</p> <ol> <li>Aligns locus gene nucleotide sequences to the assembly contig     sequences using minimap2.</li> <li>Identifies the best matching locus type using the scoring     algorithm.</li> <li>Extracts the locus gene sequences from the assembly contig     sequences.</li> <li>Predicts the serotype/phenotype based on the gene content.</li> </ol> <p></p>"},{"location":"Method.html#scoring-algorithm","title":"Scoring algorithm","text":"<ol> <li>A matrix is initialised with one row per locus and one column per     score metric.</li> <li>For each gene found, the best alignment is chosen and the scores are     added to the respective locus row if the coverage passes the     threshold (<code>--min-cov</code>).</li> <li>The matrix is then weighted by the <code>--weight-metric</code> and the scores     are selected from the column corresponding to the <code>--score-metric</code>.</li> <li>The top N loci (<code>--n-best</code>) are selected to be fully aligned to the     assembly.</li> <li>Steps 1 and 2 are repeated and the best locus is selected from the     column corresponding to the <code>--score-metric</code>.</li> </ol> <p>The score metric can be explicitly set by the flag <code>--score-metric</code>, the options are:</p> <ul> <li><code>0</code> (<code>AS</code>) - Sum of the alignment scores per query</li> <li><code>1</code> (<code>mlen</code>) - Sum of the number of matching bases in the alignment   per query</li> <li><code>2</code> (<code>blen</code>) - Sum of the number of aligned bases per query</li> <li><code>3</code> (<code>q_len</code>) - Sum of the number of bases of each query found   (regardless of whether they are aligned)</li> </ul> <p>The weighting can be explicitly set by the flag <code>--weight-metric</code>; the options are:</p> <ul> <li><code>0</code> - No weighting</li> <li><code>1</code> - Number of genes found</li> <li><code>2</code> - Number of genes expected</li> <li><code>3</code> - Proportion of genes found</li> <li><code>4</code> - Sum of the number of aligned bases per query (<code>blen</code>)</li> <li><code>5</code> - Sum of the number of bases of each query found (regardless of   whether they are aligned) (<code>q_len</code>)</li> </ul> <p>Note</p> <p>The gene score matrix can be written to a TSV file using the <code>--scores</code> flag, however this will not type the assembly or reconstruct the locus.</p> <p></p>"},{"location":"Method.html#locus-reconstruction","title":"Locus reconstruction","text":"<p>After the best matching locus type has been identified, Kaptive will:</p> <ol> <li> <p>For each contig, the ranges from the full-length locus alignments of     the best match are extracted.</p> </li> <li> <p>The ranges are merged together if they are within the distance of     the largest locus in the database.</p> </li> <li> <p>The merged ranges are used to create <code>LocusPiece</code> objects and the     sequence is extracted from the assembly contig.</p> </li> <li> <p>Gene alignments are culled twice to determine the gene content:  </p> <ol> <li>The first removes alignments overlapping genes from the best     match.</li> <li>The remaining alignments that are not part of the best match are     culled so that the best alignment is kept and each alignment     represents the best gene for that part of the contig.</li> </ol> </li> <li> <p>For each remaining gene alignment, the gene is then evaluated.</p> </li> </ol> <p>Note</p> <p>You may see multiple entries for the same gene in the gene details columns. This can happen when there are multiple significant alignments for a gene that has been split, either over contigs or by an artefact such as an insertion element. This will be evident by the low percent coverage of the entries, which should all add up to ~100% if they are indeed part of the same gene.</p> <p></p>"},{"location":"Method.html#gene-evaluation","title":"Gene evaluation","text":"<p>For each <code>GeneResult</code> object, Kaptive will:</p> <ol> <li>Check whether the gene is partial by determining if the gene     overlaps the contig boundaries.</li> <li>Extract the DNA sequence from the assembly contig and translate to     amino acid.</li> <li>Perform pairwise alignment to the reference gene amino acid sequence     and calculate percent identity.</li> <li>Check for truncation by determining if the amino acid sequence     length is &lt;95% of the reference gene protein length.</li> <li>Determine whether the gene belongs to the biosynthetic gene cluster     (inside locus) or not (outside locus).</li> </ol> <p>Note</p> <p>Partial genes are not considered for truncation. This prevents false positive truncation calls in fragmented assemblies which may otherwise have an impact on phenotype prediction, but may result in underreporting of genuine truncations e.g. due to insertion sequence insertion.</p> <p></p>"},{"location":"Method.html#phenotype-prediction","title":"Phenotype prediction","text":"<p>As of Kaptive 3, we have added the ability to predict the resulting phenotype of the assembly. This is similar to how the Type was reported in previous versions, but now includes the ability to predict specific phenotypes based on known mutations/modifications in a given set of locus genes as defined in the database logic file.</p>"},{"location":"Method.html#confidence-score","title":"Confidence score","text":"<p>Kaptive with finally calculate how confident it is in the prediction, which is explained here.</p>"},{"location":"Outputs.html","title":"Outputs","text":""},{"location":"Outputs.html#tabular","title":"Tabular","text":"<p>The main output of the assembly typing mode is a tab-delimited table of the results with the following columns:</p> Column name Description Assembly The name of the input assembly, taken from the assembly filename. Best match locus The locus type which most closely matches the assembly. Best match type The predicted serotype/phenotype of the assembly. Match confidence A categorical measure of locus call quality (see confidence score). Problems Characters indicating issues with the locus match (see problems). Identity Weighted percent identity of the best matching locus to the assembly. Coverage Weighted percent coverage of the best matching locus in the assembly. Length discrepancy If the locus was found in a single piece, this is the difference between the locus length and the assembly length. Expected genes in locus A fraction indicating how many of the genes in the best matching locus were found in the locus part of the assembly. Expected genes in locus, details Gene names for the expected genes found in the locus part of the assembly. Missing expected genes A string listing the gene names of expected genes that were not found. Other genes in locus The number of unexpected genes (genes from loci other than the best match) which were found in the locus part of the assembly. Other genes in locus, details Gene names for the other genes found in the locus part of the assembly. Expected genes outside locus A fraction indicating how many of the expected genes which were found in the assembly but not in the locus part of the assembly (usually zero) Expected genes outside locus, details Gene names for the expected genes found outside the locus part of the assembly. Other genes outside locus The number of unexpected genes (genes from loci other than the best match) which were found outside the locus part of the assembly. Other genes outside locus, details Gene names for the other genes found outside the locus part of the assembly. Truncated genes, details Gene names for the truncated genes found in the assembly. Extra genes, details Gene names for the extra genes found in the assembly. <p>Note</p> <p>Numbers beside gene names indicate the percent identity and percent coverage of the gene in the assembly.</p> <p>Note</p> <p>You may sometimes see two copies of the same gene in the <code>Expected genes in locus, details</code> column. These represent (likely) parts of the same gene which have usually been split over contigs. In Kaptive v3.0.0 onwards, we adopted this behaviour to allow users to see where locus splitting has occurred, and determine the total percent identity of a gene that has been split.</p> <p>The default is to print this table to stdout. You can use UNIX redirection operators (<code>&gt;</code> or <code>&gt;&gt;</code>) or the <code>-o</code>/<code>--out</code> flag to write to a file.</p> <p>If the summary table already exists and is not empty, Kaptive will append to it (not overwrite it) and suppress the header line. This allows you to run Kaptive in succession on sets of assemblies, all outputting to the same table file.</p> <p>To disable the tabular output, simply redirect the output to <code>/dev/null</code>.</p> <p></p>"},{"location":"Outputs.html#fasta","title":"Fasta","text":"<p>The <code>-f/--fasta</code> flag produces a fasta file of the region(s) of the assembly which correspond to the best locus match. This may be a single piece (in cases of a good assembly and a strong match) or it may be in multiple pieces (in cases of poor assembly and/or a novel locus).</p> <p>You can specify either a directory, which will write one file per assembly named as <code>{assembly}_kaptive_results.fna</code>, or a single file (\"-\" for <code>stdout</code>), which will write all the sequences to that file.</p> <p>For example:</p> <pre><code>kaptive assembly kpsc_k assembly.fasta -f\n</code></pre> <p>This results in default behaviour which will produce one file per assembly in the current directory. However, to specify a directory:</p> <pre><code>kaptive assembly kpsc_k assembly.fasta -f kaptive_results/\n</code></pre> <p>or for a single file, both are valid:</p> <pre><code>kaptive assembly kpsc_k assembly.fasta -f kaptive_results.fna\nkaptive assembly kpsc_k assembly.fasta -f - &gt; kaptive_results.fna\n</code></pre> <p>Note</p> <p>This is the same as the <code>--fna</code> flag in <code>kaptive convert</code>.</p> <p></p>"},{"location":"Outputs.html#json","title":"JSON","text":"<p>The <code>-j/--json</code> flag produces a JSON file of the results which allows Kaptive to reconstruct the <code>TypingResult</code> objects after a run which can be used with kaptive-convert. Unlike previous version (2 and below), this is a JSON lines file (or \"-\" for <code>stdout</code>), where each line is a JSON object representing the results for a single assembly. If the file already exists, Kaptive will append to it (not overwrite it).</p> <p>The default is to write this file to: <code>kaptive_results.json</code>, however the path can be specified after the flag, for example:</p> <pre><code>kaptive assembly kpsc_k assembly.fasta -j kaptive_results.json\n</code></pre> <p>Warning</p> <p>It is possible to write all text formats (TSV, JSON and FASTA) to the same file (including stdout), however this is not recommended for downstream analysis.</p>"},{"location":"Usage.html","title":"Usage","text":""},{"location":"Usage.html#quickstart-for-the-impatient","title":"Quickstart (for the impatient)","text":"<p>To type polysaccharide loci from genome assemblies:</p> kaptive assembly &lt;db&gt; assemblies/*.fasta &gt; results.tsv <p>This will run <code>kaptive assembly</code> with the default parameters, and produce a table detailing the best match locus, predicted phenotype, confidence score and detailed typing information for each input genome assembly in the file called <code>kaptive_results.tsv</code>.</p>"},{"location":"Usage.html#detailed-usage","title":"Detailed usage","text":"<p>We designed Kaptive 3 to be easier to use on the command-line than previous versions by structuring the program as a series of sub-commands that follow the general pattern of <code>kaptive &lt;mode&gt; &lt;database&gt; &lt;input&gt;</code>. There are three modes:</p> <ul> <li>assembly: type assemblies</li> <li>convert: convert Kaptive results to   different formats</li> <li>extract: extract features from   Kaptive databases in different formats</li> </ul> <p>Note</p> <p>To see the full list of commands and options, run <code>kaptive -h/--help</code>.</p> <p></p>"},{"location":"Usage.html#kaptive-assembly","title":"kaptive assembly","text":"<p>Given a Kaptive database and a bacterial genome assembly, <code>kaptive assembly</code> will perform 3 main tasks:</p> <ul> <li>Determines the most likely locus type of the genome assembly.</li> <li>Reconstructs the biosynthetic gene cluster from the assembly contig   sequences.</li> <li>Predicts the corresponding serotype/phenotype of the genome assembly.</li> </ul> <p>Note</p> <p>As of version 3, Kaptive no longer supports allelic (wzi, wzc) typing.</p> <p>To perform K locus typing on a directory of Klebsiella pneumoniae assemblies, you would run:</p> kaptive assembly kpsc_k assemblies/*.fasta &gt; results.tsv <p>Here we have told Kaptive to perform typing of assemblies with <code>assembly</code> and used the database keyword <code>kpsc_k</code> to specify the Klebsiella pneumoniae K locus database. All other parameters are set to the default.</p> <p>Database keywords are a handy short-cut for using the databases distributed with Kaptive and located in the Kaptive package data. Alternatively, you can specify the full path to your own database.</p> <p>You may also want to specify the locations and/or filenames of the output files using the following options:</p> <pre><code>Note, text outputs accept '-' for stdout\n\n-o , --out           Output file to write/append tabular results to (default: stdout)\n-f [], --fasta []    Turn on fasta output\n                     Accepts a single file or a directory (default: cwd)\n-j [], --json []     Turn on JSON lines output\n                     Optionally choose file (can be existing) (default: kaptive_results.json)\n-s [], --scores []   Dump locus score matrix to tsv (typing will not be performed!)\n                     Optionally choose file (can be existing) (default: stdout)\n--no-header          Suppress header line\n</code></pre> <p>Example:</p> kaptive assembly kpsc_k assemblies/*.fasta \\   -o kaptive_results.tsv -f -j <p>This will output a tabular file called <code>kaptive_results.tsv</code>, a fasta file for each assembly called <code>{assembly}_kaptive_results.fna</code> and a JSON lines file called <code>kaptive_results.json</code>.</p> <p>Warning</p> <p>It is possible to write all text formats (TSV, JSON and FASTA) to the same file (including stdout), however this is not recommended for downstream analysis.</p>"},{"location":"Usage.html#advanced-options","title":"Advanced options","text":"<p>Advanced users may wish to customise Kaptive's scoring options (for picking the best match locus), confidence options (for marking matches as 'Typeable' or 'Untypeable') or database parsing options. We recommend keeping the default options for standard typing using the Klebsiella and/or A. baumanii databases distributed with Kaptive.</p> <p>Scoring options:</p> <pre><code>--min-cov            Minimum gene %coverage (blen/q_len*100) to be used for scoring (default: 50.0)\n--score-metric       Metric for scoring each locus (default: 0)\n                       0: AS (alignment score of genes found)\n                       1: mlen (matching bases of genes found)\n                       2: blen (aligned bases of genes found)\n                       3: q_len (query length of genes found)\n--weight-metric      Weighting for the 1st stage of the scoring algorithm (default: 3)\n                       0: No weighting\n                       1: Number of genes found\n                       2: Number of genes expected\n                       3: Proportion of genes found\n                       4: blen (aligned bases of genes found)\n                       5: q_len (query length of genes found)\n--n-best             Number of best loci from the 1st round of scoring to be\n                     fully aligned to the assembly (default: 2)\n</code></pre> <p></p> <p>Confidence options:</p> <pre><code>--gene-threshold     Species-level locus gene identity threshold (default: database specific)\n--max-other-genes    Typeable if &lt;= other genes (default: 1)\n--percent-expected   Typeable if &gt;= % expected genes (default: 50)\n--below-threshold    Typeable if any genes are below threshold (default: False)\n</code></pre> <p>See database options here and other options:</p> <pre><code>-V, --verbose         Print debug messages to stderr\n-v , --version        Show version number and exit\n-h , --help           Show this help message and exit\n-t , --threads        Number of threads for alignment (default: maximum available CPUs / 32)\n</code></pre> <p></p>"},{"location":"Usage.html#kaptive-convert","title":"kaptive convert","text":"<p>The <code>convert</code> command allows you to convert the Kaptive results JSON file into a range of useful formats, including:</p> <ul> <li>tsv: Tabular output (tsv)</li> <li>json: JSON lines format (same as input but optionally filtered)</li> <li>fna: Locus nucleotide sequences in fasta format.</li> <li>ffn: Gene nucleotide sequences in fasta format.</li> <li>faa: Protein sequences in fasta format.</li> </ul> <p>Warning</p> <p>The <code>convert</code> command is only compatible with JSON files from Kaptive v3.0.0 onwards.</p>"},{"location":"Usage.html#usage_1","title":"Usage","text":"kaptive convert -h... <p>General usage is as follows:</p> <pre><code>kaptive convert &lt;db&gt; &lt;json&gt; [formats] [options]\n</code></pre> <p>Inputs:</p> <pre><code>db path/keyword       Kaptive database path or keyword\njson                  Kaptive JSON lines file or - for stdin\n</code></pre> <p>Formats:</p> <pre><code>Note, text outputs accept '-' for stdout\n\n-t [], --tsv []       Convert to tabular format in file (default: stdout)\n-j [], --json []      Convert to JSON lines format in file (default: stdout)\n--fna []              Convert to locus nucleotide sequences in fasta format\n                      Accepts a single file or a directory (default: cwd)\n--ffn []              Convert to locus gene nucleotide sequences in fasta format\n                      Accepts a single file or a directory (default: cwd)\n--faa []              Convert to locus gene protein sequences in fasta format\n                      Accepts a single file or a directory (default: cwd)\n--no-header           Suppress header line\n</code></pre> <p>Filter options:</p> <pre><code>-r , --regex          Python regular-expression to select JSON lines (default: All)\n-l  [ ...], --loci  [ ...]\n                    Space-separated list to filter locus names (default: All)\n-s  [ ...], --samples  [ ...]\n                    Space-separated list to filter sample names (default: All)\n</code></pre> <p>Note</p> <p>Filters take precedence in descending order</p> <p>For example, to convert the JSON file to a tabular format, run either of the following commands:</p> <pre><code>kaptive convert kpsc_k kaptive_results.json --tsv kaptive_results.tsv\n\ncat *.json | kaptive convert kpsc_k - --tsv - &gt; kaptive_results.tsv\n</code></pre> <p>To output multiple formats, you can run:</p> <pre><code>kaptive convert kpsc_k kaptive_results.json --tsv kaptive_results.tsv --fna - --faa proteins/\n</code></pre> <p>Where the tabular results will be written to <code>kaptive_results.tsv</code>, the locus nucleotide sequences will be written to stdout, and the protein sequences will be written to the directory <code>proteins/</code> with the filenames <code>{assembly}_kaptive_results.faa</code>.</p> <p>Warning</p> <p>It is possible to write all text formats (TSV, JSON, FNA, FAA and FFN) to the same file (including stdout), however this is not recommended for downstream analysis.</p> <p></p>"},{"location":"Usage.html#kaptive-extract","title":"kaptive extract","text":"<p>Kaptive 3.0.0 and above includes a new command-line mode <code>extract</code> that allows you to extract features from a Kaptive database in the following formats:</p> <ul> <li>fna: Locus nucleotide sequences in fasta format.</li> <li>ffn: Gene nucleotide sequences in fasta format.</li> <li>faa: Protein sequences in fasta format.</li> </ul>"},{"location":"Usage.html#usage_2","title":"Usage","text":"<p>General usage is as follows:</p> <pre><code>kaptive extract &lt;db&gt; [formats] [options]\n</code></pre> <p>Formats:</p> <pre><code>Note, text outputs accept '-' for stdout\n\n--fna []         Convert to locus nucleotide sequences in fasta format\n                 Accepts a single file or a directory (default: cwd)\n--ffn []         Convert to locus gene nucleotide sequences in fasta format\n                 Accepts a single file or a directory (default: cwd)\n--faa []         Convert to locus gene protein sequences in fasta format\n                 Accepts a single file or a directory (default: cwd)\n</code></pre> <p></p> <p>Database options:</p> <pre><code>--locus-regex    Python regular-expression to match locus names in db source note\n--type-regex     Python regular-expression to match locus types in db source note\n--filter         Python regular-expression to select loci to include in the database\n</code></pre> <p>Note</p> <p>These options are useful for customising the database to your needs, for example, to include only a subset of loci or to change the way locus names and types are parsed from the source note.</p> <p>Other options:</p> <pre><code>-V, --verbose    Print debug messages to stderr\n-v , --version   Show version number and exit\n-h , --help      Show this help message and exit\n</code></pre> <p>For example, to extract the gene nucleotide sequences from the Klebsiella pneumoniae K locus primary reference database in fasta format, run:</p> <pre><code>kaptive extract kp_k --fna k_loci.fna\n</code></pre> <p>To extract all protein sequences from KL1 and KL2, run either one of the following:</p> <pre><code>kaptive extract kp_k --filter \"^KL(1|2)$\" --faa KL1_KL2_proteins.faa\nkaptive extract kp_k --filter \"^KL(1|2)$\" --faa - &gt; KL1_KL2_proteins.faa\n</code></pre> <p>To do the same but output each locus to a separate file, run either:</p> <pre><code>kaptive extract kp_k --filter \"^KL(1|2)$\" --faa\nkaptive extract kp_k --filter \"^KL(1|2)$\" --faa protein_files/\n</code></pre> <p>Which would create two files: <code>KL1.faa</code> and <code>KL2.faa</code>.</p> <p>kaptive assembly kpsc_k assembly.fasta -j kaptive_results.json</p> <p>Warning</p> <p>It is possible to write all text formats (<code>fna</code>, <code>faa</code> and <code>ffn</code>) to the same file (including stdout), however this is not recommended for downstream analysis.</p> <p></p>"},{"location":"Usage.html#api","title":"API","text":"<p>Whilst Kaptive isn't designed to be a fully-fledged API, it is possible to use it as a module in your own Python scripts. For typing assemblies, you can use the <code>kaptive.assembly.typing_pipeline</code> function, which takes an assembly and a <code>kaptive.database.Database</code> object as input and returns a <code>kaptive.typing.TypingResult</code> object.</p> <pre><code>from kaptive.assembly import typing_pipeline\nfrom kaptive.database import load_database\nfrom pathlib import Path\n\ndb = load_database('kpsc_k')  ## Load the Klebsiella K locus database once and pass it to the typing pipeline\nfor result in map(lambda a: typing_pipeline(a, db), Path('assemblies').glob('*.fna.gz')):\n    if result:  ## If the assembly was successfully typed\n        print(result.format('tsv'), end='')  ## TSV format will end in a newline, so we set end to ''\n</code></pre> <p>For example, if you wanted to perform K and O locus typing on a single assembly, you could do the following:</p> <pre><code>## Here, we pass the keyword arguments for the database, they will be loaded inside the typing pipeline\nfor result in map(lambda d: typing_pipeline('test/kpsc/2018-01-389.fasta', d), ['kpsc_k', 'kpsc_o']):\n    if result:  ## If the assembly was successfully typed\n        print(result.format('tsv'), end='')  ## TSV format will end in a newline, so we set end to ''\n</code></pre> <p>Note</p> <p>By default the <code>typing_pipeline</code> runs <code>minimap2</code> on a all available CPUs, however this can be controlled with the <code>threads</code> parameter.</p>"},{"location":"reference/kaptive/index.html","title":"kaptive","text":""},{"location":"reference/kaptive/index.html#kaptive","title":"<code>kaptive</code>","text":""},{"location":"reference/kaptive/alignment.html","title":"alignment","text":""},{"location":"reference/kaptive/alignment.html#kaptive.alignment","title":"<code>kaptive.alignment</code>","text":""},{"location":"reference/kaptive/alignment.html#kaptive.alignment.Alignment","title":"<code>Alignment</code>","text":"<p>Similar to <code>mappy.Alignment</code> but with additional attributes and methods.</p> Source code in <code>kaptive/alignment.py</code> <pre><code>class Alignment:\n    \"\"\"\n    Similar to `mappy.Alignment` but with additional attributes and methods.\n    \"\"\"\n\n    def __init__(\n            self, q: str = None, q_len: int | None = 0, q_st: int | None = 0,\n            q_en: int | None = 0, strand: str = None, ctg: str = None,\n            ctg_len: int | None = 0, r_st: int | None = 0, r_en: int | None = 0,\n            mlen: int | None = 0, blen: int | None = 0, mapq: int | None = 0,\n            tags: dict = None):\n        self.q = q or 'unknown'  # Query sequence name\n        self.q_len = q_len  # Query sequence length\n        self.q_st = q_st  # Query start coordinate (0-based)\n        self.q_en = q_en  # Query end coordinate (0-based)\n        self.strand = strand or 'unknown'  # \u2018+\u2019 if query/target on the same strand; \u2018-\u2019 if opposite\n        self.ctg = ctg or 'unknown'  # Target sequence name\n        self.ctg_len = ctg_len  # Target sequence length\n        self.r_st = r_st  # Target start coordinate on the original strand (0-based)\n        self.r_en = r_en  # Target end coordinate on the original strand (0-based)\n        self.mlen = mlen  # Number of matching bases in the alignment\n        self.blen = blen  # Number bases, including gaps, in the alignment\n        self.mapq = mapq  # Mapping quality (0-255 with 255 for missing)\n        self.tags = tags or {}  # {tag: value} pairs\n\n    @classmethod\n    def from_paf_line(cls, line: str):\n        \"\"\"\n        Parse a line in PAF format and return an Alignment object.\n        \"\"\"\n        if len(line := line.split('\\t')) &lt; 12:\n            raise AlignmentError(f\"Line has &lt; 12 columns: {line}\")\n        try:\n            return Alignment(  # Parse standard fields\n                q=line[0], q_len=int(line[1]), q_st=int(line[2]), q_en=int(line[3]), strand=line[4], ctg=line[5],\n                ctg_len=int(line[6]), r_st=int(line[7]), r_en=int(line[8]), mlen=int(line[9]), blen=int(line[10]),\n                mapq=int(line[11]),\n                tags={(x := t.split(\":\", 2))[0]: int(x[2]) if x[1] == \"i\" else float(x[2]) if x[1] == \"f\" else x[2] for\n                      t in line[12:]}\n            )\n        except Exception as e:\n            raise AlignmentError(f\"Error parsing PAF line: {line}\") from e\n\n    def __repr__(self):\n        return f'{self.q}:{self.q_st}-{self.q_en} {self.ctg}:{self.r_st}-{self.r_en} {self.strand}'\n\n    def __len__(self):\n        return self.num_bases\n\n    def __getattr__(self, item):\n        if item in self.__dict__:  # First check attributes\n            return self.__dict__[item]\n        elif item in self.tags:  # Then check tags\n            return self.tags[item]\n        else:\n            raise AttributeError(f\"{self.__class__.__name__} object has no attribute {item}\")\n\n    @property\n    def partial(self) -&gt; bool:\n        if self.q_len &gt; self.ctg_len:\n            return True\n        if (self.r_en == self.ctg_len or self.r_st == 0) and (self.q_en - self.q_st) &lt; self.q_len:\n            return True\n        ref_start_diff = self.r_st\n        ref_end_diff = self.ctg_len - self.r_en\n        query_start_diff = self.q_st if self.strand == '+' else self.q_len - self.q_en\n        query_end_diff = self.q_len - self.q_en if self.strand == '+' else self.q_st\n        if query_start_diff &gt; ref_start_diff or query_end_diff &gt; ref_end_diff:\n            return True\n        return False\n</code></pre>"},{"location":"reference/kaptive/alignment.html#kaptive.alignment.Alignment.from_paf_line","title":"<code>from_paf_line(line)</code>  <code>classmethod</code>","text":"<p>Parse a line in PAF format and return an Alignment object.</p> Source code in <code>kaptive/alignment.py</code> <pre><code>@classmethod\ndef from_paf_line(cls, line: str):\n    \"\"\"\n    Parse a line in PAF format and return an Alignment object.\n    \"\"\"\n    if len(line := line.split('\\t')) &lt; 12:\n        raise AlignmentError(f\"Line has &lt; 12 columns: {line}\")\n    try:\n        return Alignment(  # Parse standard fields\n            q=line[0], q_len=int(line[1]), q_st=int(line[2]), q_en=int(line[3]), strand=line[4], ctg=line[5],\n            ctg_len=int(line[6]), r_st=int(line[7]), r_en=int(line[8]), mlen=int(line[9]), blen=int(line[10]),\n            mapq=int(line[11]),\n            tags={(x := t.split(\":\", 2))[0]: int(x[2]) if x[1] == \"i\" else float(x[2]) if x[1] == \"f\" else x[2] for\n                  t in line[12:]}\n        )\n    except Exception as e:\n        raise AlignmentError(f\"Error parsing PAF line: {line}\") from e\n</code></pre>"},{"location":"reference/kaptive/alignment.html#kaptive.alignment.cull","title":"<code>cull(keep, alignments, overlap_fraction=0.1)</code>","text":"<p>Yield alignments that do not conflict with keep alignment</p> Source code in <code>kaptive/alignment.py</code> <pre><code>def cull(keep: Alignment, alignments: Iterable[Alignment],\n         overlap_fraction: float = 0.1) -&gt; Generator[Alignment]:\n    \"\"\"Yield alignments that do not conflict with keep alignment\"\"\"\n    for a in alignments:\n        if (a.ctg != keep.ctg or  # Different contig\n                range_overlap((a.r_st, a.r_en), (keep.r_st, keep.r_en), skip_sort=True) / a.blen &lt; overlap_fraction):\n            yield a\n</code></pre>"},{"location":"reference/kaptive/alignment.html#kaptive.alignment.cull_filtered","title":"<code>cull_filtered(pred, alignments)</code>","text":"<p>Cull and flatten alignments that don't overlap with alignments matching the predicate. E.g. list(cull_filtered(lambda i: i.q in query_genes, alignments))</p> Source code in <code>kaptive/alignment.py</code> <pre><code>def cull_filtered(pred: callable, alignments: Iterable[Alignment]) -&gt; Generator[Alignment]:\n    \"\"\"\n    Cull and flatten alignments that don't overlap with alignments matching the predicate.\n    E.g. list(cull_filtered(lambda i: i.q in query_genes, alignments))\n    \"\"\"\n    keep, other = [], []\n    [(keep if pred(a) else other).append(a) for a in alignments]\n    other = cull_all(other)  # Remove conflicting other alignments\n    for i in keep:  # Remove other alignments overlapping best match gene alignments\n        other = list(cull(i, other))\n        yield i\n    yield from other\n</code></pre>"},{"location":"reference/kaptive/alignment.html#kaptive.alignment.group_alns","title":"<code>group_alns(alignments, key='q')</code>","text":"<p>Group alignments by a key</p> Source code in <code>kaptive/alignment.py</code> <pre><code>def group_alns(alignments: Iterable[Alignment], key: str = 'q') -&gt; Generator[tuple[str, Generator[Alignment]]]:\n    \"\"\"Group alignments by a key\"\"\"\n    yield from groupby(sorted(alignments, key=lambda x: getattr(x, key)), key=lambda x: getattr(x, key))\n</code></pre>"},{"location":"reference/kaptive/assembly.html","title":"assembly","text":""},{"location":"reference/kaptive/assembly.html#kaptive.assembly","title":"<code>kaptive.assembly</code>","text":""},{"location":"reference/kaptive/assembly.html#kaptive.assembly.Contig","title":"<code>Contig</code>","text":"<p>               Bases: <code>object</code></p> <p>This class describes a contig in an assembly: the name, length, and sequence.</p> Source code in <code>kaptive/assembly.py</code> <pre><code>class Contig(object):\n    \"\"\"\n    This class describes a contig in an assembly: the name, length, and sequence.\n    \"\"\"\n\n    def __init__(self, name: str, desc: str, seq: Seq):\n        self.name = name\n        self.desc = desc\n        self.seq = seq\n\n    def __repr__(self):\n        return self.name\n\n    def __len__(self):\n        return len(self.seq)\n</code></pre>"},{"location":"reference/kaptive/assembly.html#kaptive.assembly.parse_assembly","title":"<code>parse_assembly(file, verbose=False)</code>","text":"<p>Parse an assembly file and return an Assembly object</p> Source code in <code>kaptive/assembly.py</code> <pre><code>def parse_assembly(file: PathLike | str, verbose: bool = False) -&gt; Assembly | None:\n    \"\"\"Parse an assembly file and return an Assembly object\"\"\"\n    if file := check_file(file):  # Check the file exists, warn if not (instead of quitting)\n        if match := _ASSEMBLY_FASTA_REGEX.search(basename := path.basename(file)):\n            log(f'Assuming {basename} is in fasta format', verbose=verbose)\n            assembly = Assembly(file, basename.rstrip(match.group()))\n            try:\n                with opener(file, verbose=verbose, mode='rt') as f:\n                    for header, seq in SimpleFastaParser(f):\n                        header = header.split(maxsplit=1)\n                        name, description = header if len(header) == 2 else (header[0], '')\n                        assembly.contigs[name] = Contig(name, description, Seq(seq))\n            except Exception as e:\n                return warning(f\"Error parsing {basename}\\n{e}\")\n            return assembly\n        return warning(f\"File extension must match {_ASSEMBLY_FASTA_REGEX.pattern}: {basename}\")\n</code></pre>"},{"location":"reference/kaptive/assembly.html#kaptive.assembly.typing_pipeline","title":"<code>typing_pipeline(assembly, db, threads=0, score_metric=0, weight_metric=3, min_cov=50, n_best=2, max_other_genes=1, percent_expected_genes=50, allow_below_threshold=False, score_file=None, verbose=False)</code>","text":"<p>Performs in silico serotyping on a bacterial genome assembly using a database of known loci. :param assembly: Path to the assembly file or Assembly object :param db: Path to the database file or Database object :param threads: Number of threads to use for alignment :param score_metric:  score to use: 0=AS, 1=mlen, 2=blen, 3=q_len :param weight_metric: Score weighting metric: 0=None, 1=Genes found, 2=Genes expected, 3=Prop genes, 4=blen, 5=q_len :param min_cov: Minimum coverage for a gene to be used for scoring :param n_best: Number of top loci from the 1st round of scoring to be fully aligned to the assembly :param max_other_genes: Max other genes to allow in the best locus to be considered Typeable :param percent_expected_genes: Percent of expected genes required to be considered Typeable :param allow_below_threshold: Allow genes below the threshold to be considered Typeable :param score_file: File handle to write the scores to, will not type the assembly if provided :param verbose: Print progress to stderr :return: TypingResult object or None</p> Source code in <code>kaptive/assembly.py</code> <pre><code>def typing_pipeline(\n        assembly: str | PathLike | Assembly, db: str | PathLike | Database, threads: int = 0,\n        score_metric: int = 0, weight_metric: int = 3, min_cov: float = 50, n_best: int = 2,\n        max_other_genes: int = 1, percent_expected_genes: float = 50, allow_below_threshold: bool = False,\n        score_file: TextIO = None, verbose: bool = False) -&gt; TypingResult | None:\n    \"\"\"\n    Performs *in silico* serotyping on a bacterial genome assembly using a database of known loci.\n    :param assembly: Path to the assembly file or Assembly object\n    :param db: Path to the database file or Database object\n    :param threads: Number of threads to use for alignment\n    :param score_metric:  score to use: 0=AS, 1=mlen, 2=blen, 3=q_len\n    :param weight_metric: Score weighting metric: 0=None, 1=Genes found, 2=Genes expected, 3=Prop genes, 4=blen, 5=q_len\n    :param min_cov: Minimum coverage for a gene to be used for scoring\n    :param n_best: Number of top loci from the 1st round of scoring to be fully aligned to the assembly\n    :param max_other_genes: Max other genes to allow in the best locus to be considered Typeable\n    :param percent_expected_genes: Percent of expected genes required to be considered Typeable\n    :param allow_below_threshold: Allow genes below the threshold to be considered Typeable\n    :param score_file: File handle to write the scores to, will not type the assembly if provided\n    :param verbose: Print progress to stderr\n    :return: TypingResult object or None\n    \"\"\"\n    # CHECK ARGS -------------------------------------------------------------------------------------------------------\n    if not isinstance(db, Database) and not (db := load_database(db, verbose=verbose)):\n        return None\n    if not isinstance(assembly, Assembly) and not (assembly := parse_assembly(assembly, verbose=verbose)):\n        return None\n    threads = threads if threads else check_cpus(threads, verbose=verbose)\n    # ALIGN GENES ------------------------------------------------------------------------------------------------------\n    # Init scores array with 6 columns: AS, mlen, blen, q_len, genes_found, genes_expected\n    scores, alignments = np.zeros((len(db), 6)), []\n    # Group alignments by query gene (Alignment.q)\n    for q, alns in group_alns(assembly.map(db.format('ffn'), threads, verbose=verbose)):\n        if q.startswith(\"Extra_genes\"):\n            alignments.append(max(alns, key=lambda x: x.mlen))  # Add the best alignment for extra genes\n        else:\n            alignments.extend(alns := list(alns))  # Add all alignments to the list, convert generator to list too\n            # Use the best alignment for each gene for scoring, if the coverage is above the minimum\n            if ((best := max(alns, key=lambda x: x.mlen)).blen / best.q_len) * 100 &gt;= min_cov:\n                scores[db.loci[q.split('_', 1)[0]].index] += [\n                    best.tags['AS'], best.mlen, best.blen, best.q_len, 1, 0]\n            # For each gene, add: AS, mlen, blen, q_len, genes_found (1), genes_expected (0 but will update later)\n\n    if scores.max() == 0:  # If no gene alignments were found, return None so pipeline can continue\n        return warning(f'No gene alignments sufficient for typing {assembly}\\n'\n                       f'Have you used the appropriate database for your species?')\n\n    # SCORE LOCI -------------------------------------------------------------------------------------------------------\n    scores[:, 5] = db.expected_gene_counts  # Add expected genes to the 6th column (0-based) score matrix\n\n    if score_file:  # If we are just scoring the assembly\n        score_file.write(  # Write the scores to the file\n            ''.join([f\"{assembly}\\t{k}\\t\" + '\\t'.join(map(str, v)) + '\\n' for k, v in zip(db.loci.keys(), scores)])\n        )\n        return log(f\"Finished scoring {assembly}\", verbose=verbose)  # Return without typing the assembly\n\n    # Process the scores to get the best loci to fully align, this collapses the matrix to a 1D array\n    if weight_metric:  # If we are using a weighted score\n        if weight_metric == 1:\n            scores = scores[:, score_metric] / scores[:, 4]  # Genes found\n        elif weight_metric == 2:\n            scores = scores[:, score_metric] / scores[:, 5]  # Genes expected\n        elif weight_metric == 3:\n            scores = scores[:, score_metric] * (scores[:, 4] / scores[:, 5])  # Prop genes\n        elif weight_metric == 4:\n            scores = scores[:, score_metric] / scores[:, 2]  # blen\n        elif weight_metric == 5:\n            scores = scores[:, score_metric] / scores[:, 3]  # q_len\n    else:\n        scores = scores[:, score_metric]  # Unweighted score\n\n    best_loci = [db[int(i)] for i in\n                 np.argsort(scores)[::-1][:min(n_best, len(scores))]]  # Get the best loci to fully align\n    scores, idx = np.zeros((len(best_loci), 4)), {l.name: i for i, l in enumerate(best_loci)}  # Init scores and index\n    locus_alignments = {l.name: [] for l in best_loci}  # Init dict to store alignments for each locus\n    # Group alignments by locus\n    for locus, alns in group_alns(assembly.map(''.join(i.format('fna') for i in best_loci), threads, verbose=verbose)):\n        for a in alns:  # For each alignment of the locus\n            scores[idx[locus]] += [a.tags['AS'], a.mlen, a.blen, a.q_len]  # Add alignment metrics to the scores\n            locus_alignments[locus].append(a)  # Add the alignment to the locus alignments\n    best_match = best_loci[np.argmax(scores[:, score_metric])]  # Get the best match based on the highest score\n\n    # RECONSTRUCT LOCUS ------------------------------------------------------------------------------------------------\n    result = TypingResult(assembly.name, db, best_match)  # Create the result object\n    pieces = {  # Init dict to store pieces for each contig\n        ctg: [LocusPiece(ctg, result, s, e) for s, e in  # Create pieces for each merged contig range\n              merge_ranges([(a.r_st, a.r_en) for a in alns], len(db.largest_locus))]  # Merge ranges by largest locus\n        for ctg, alns in group_alns(locus_alignments[best_match.name], key='ctg')  # Group by contig\n    }  # We can't add strand as the pieces may be merged from multiple alignments, we will determine from the genes\n\n    # GET GENE RESULTS -------------------------------------------------------------------------------------------------\n    for a in cull_filtered(lambda i: i.q in best_match.genes, alignments):  # For each non-overlapping gene alignment\n        if gene := best_match.genes.get(a.q):  # Get gene reference from database and gene type\n            gene_type = \"expected_genes\"\n        elif gene := db.extra_genes.get(a.q):\n            gene_type = \"extra_genes\"\n        else:\n            gene = db.genes.get(a.q)\n            gene_type = \"unexpected_genes\"\n\n        # Get Piece if gene range overlaps with a piece\n        piece = next(filter(lambda p: range_overlap((p.start, p.end), (a.r_st, a.r_en)) &gt; 0,\n                            pieces.get(a.ctg, [])), None)\n        # Create gene result and extract sequence from assembly\n        gene_result = GeneResult(a.ctg, gene, result, piece, a.r_st, a.r_en, a.strand, gene_type=gene_type,\n                                 partial=a.partial, dna_seq=assembly.seq(a.ctg, a.r_st, a.r_en, a.strand))\n        # Evaluate the gene in protein space by comparing the translation to the reference gene\n        gene_result.compare_translation(table=11, to_stop=True)  # This will also trigger the protein alignment\n        gene_result.below_threshold = gene_result.percent_identity &lt; db.gene_threshold  # Check if below threshold\n        if not piece and gene_result.below_threshold:  # If below protein identity threshold\n            continue  # Skip this gene, probably a homologue in another part of the genome\n        result.add_gene_result(gene_result)  # Add the gene result to the result to get neighbouring genes\n        # previous_result = gene_result  # Set the previous gene result to the current result\n\n    # FINALISE PIECES --------------------------------------------------------------------------------------------------\n    for ctg, pieces in pieces.items():  # Add sequences to pieces and add them to the result\n        for piece in pieces:\n            if piece.expected_genes:  # If the piece has expected genes\n                piece.strand = \"+\" if max(i.strand == i.gene.strand for i in piece.expected_genes) else \"-\"\n                # Piece strand is consensus of expected gene strands\n                piece.sequence = assembly.seq(ctg, piece.start, piece.end, piece.strand)\n                result.pieces.append(piece)  # Add the piece to the result\n\n    # FINALISE RESULT -------------------------------------------------------------------------------------------------\n    # Sort the pieces by the sum of the expected gene order to get the expected order of the pieces\n    result.pieces.sort(key=lambda x: min(i.gene.start for i in x.expected_genes))\n    [l.sort(key=lambda x: gene.start) for l in (\n        result.expected_genes_inside_locus, result.expected_genes_outside_locus, result.unexpected_genes_inside_locus,\n        result.unexpected_genes_outside_locus)]\n    result.missing_genes = list(set(best_match.genes) - {\n        i.gene.name for i in chain(result.expected_genes_inside_locus, result.expected_genes_outside_locus)\n    })\n    result.get_confidence(allow_below_threshold, max_other_genes, percent_expected_genes)\n    log(f\"Finished typing {result}\", verbose=verbose)\n    return result\n</code></pre>"},{"location":"reference/kaptive/assembly.html#kaptive.assembly.write_headers","title":"<code>write_headers(tsv=None, no_header=False, scores=False)</code>","text":"<p>Write appropriate header to a file handle.</p> Source code in <code>kaptive/assembly.py</code> <pre><code>def write_headers(tsv: TextIO = None, no_header: bool = False, scores: bool = False) -&gt; int:\n    \"\"\"Write appropriate header to a file handle.\"\"\"\n    if tsv and not no_header and (tsv.name == '&lt;stdout&gt;' or fstat(tsv.fileno()).st_size == 0):\n        return tsv.write(_SCORES_HEADER if scores else _ASSEMBLY_HEADER)\n</code></pre>"},{"location":"reference/kaptive/cli.html","title":"cli","text":""},{"location":"reference/kaptive/cli.html#kaptive.cli","title":"<code>kaptive.cli</code>","text":""},{"location":"reference/kaptive/cli.html#kaptive.cli.fmt_opts","title":"<code>fmt_opts(opts)</code>","text":"<p>Format opts shared by convert and extract</p> Source code in <code>kaptive/cli.py</code> <pre><code>def fmt_opts(opts: argparse.ArgumentParser):\n    \"\"\"Format opts shared by convert and extract\"\"\"\n    opts.add_argument('--fna', metavar='', nargs='?', default=None, const='.', type=check_out,\n                      help='Convert to locus nucleotide sequences in fasta format\\n'\n                           'Accepts a single file or a directory (default: cwd)')\n    opts.add_argument('--ffn', metavar='', nargs='?', default=None, const='.', type=check_out,\n                      help='Convert to locus gene nucleotide sequences in fasta format\\n'\n                           'Accepts a single file or a directory (default: cwd)')\n    opts.add_argument('--faa', metavar='', nargs='?', default=None, const='.', type=check_out,\n                      help='Convert to locus gene protein sequences in fasta format\\n'\n                           'Accepts a single file or a directory (default: cwd)')\n</code></pre>"},{"location":"reference/kaptive/cli.html#kaptive.cli.other_fmt_opts","title":"<code>other_fmt_opts(opts)</code>","text":"<p>Format opts shared by convert and assembly</p> Source code in <code>kaptive/cli.py</code> <pre><code>def other_fmt_opts(opts: argparse.ArgumentParser):\n    \"\"\"Format opts shared by convert and assembly\"\"\"\n    opts.add_argument('--no-header', action='store_true', help='Suppress header line')\n</code></pre>"},{"location":"reference/kaptive/database.html","title":"database","text":""},{"location":"reference/kaptive/database.html#kaptive.database","title":"<code>kaptive.database</code>","text":""},{"location":"reference/kaptive/database.html#kaptive.database.Database","title":"<code>Database</code>","text":"Source code in <code>kaptive/database.py</code> <pre><code>class Database:\n    def __init__(self, name: str, loci: dict[str, Locus] = None, genes: dict[str, Gene] = None,\n                 extra_genes: dict[str, Gene] = None, gene_threshold: float = None):\n        self.name = name\n        self.loci = loci or {}\n        self.genes = genes or {}\n        self.extra_genes = extra_genes or {}\n        self.gene_threshold = gene_threshold or _GENE_THRESHOLDS.get(self.name, 0)\n        self._expected_gene_counts = None\n\n    def __repr__(self):\n        return f\"{self.name} ({len(self.loci)} Loci) ({len(self.genes)} Genes) ({len(self.extra_genes)} Extra Genes)\"\n\n    def __str__(self) -&gt; str:\n        return self.name\n\n    def __len__(self) -&gt; int:\n        return len(self.loci)\n\n    def __iter__(self):\n        return iter(self.loci.values())\n\n    def __getitem__(self, item: str | int) -&gt; Locus | Gene:\n        if isinstance(item, int):\n            if not 0 &lt;= item &lt; len(self):\n                raise DatabaseError(f'Index {item} out of range for database {self.name}')\n            return list(self.loci.values())[item]\n        if not (\n        result := self.loci.get(item, self.genes.get(item, self.extra_genes.get(item)))):\n            raise DatabaseError(f'Could not find {item} in database {self.name}')\n        return result\n\n    @cached_property\n    def largest_locus(self) -&gt; Locus:\n        return max(self.loci.values(), key=len)\n\n    @property\n    def expected_gene_counts(self) -&gt; np.ndarray:\n        if self._expected_gene_counts is None:\n            self._expected_gene_counts = np.array([len(l.genes) for l in self.loci.values()])\n        return self._expected_gene_counts\n\n    def format(self, format_spec):\n        if format_spec in {'fna'}:\n            return ''.join([locus.format(format_spec) for locus in self.loci.values()])\n        elif format_spec in {'ffn', 'faa'}:\n            return ''.join([gene.format(format_spec) for gene in chain(self.genes.values(), self.extra_genes.values())])\n        else:\n            raise ValueError(f'Invalid format specifier: {format_spec}')\n\n    def add_locus(self, locus: Locus):\n        \"\"\"\n        Adds a locus and its genes to the database. Checks that the locus and genes don't already exist in the database.\n        \"\"\"\n        if not locus.name.startswith('Extra_genes'):\n            if locus.name in self.loci:\n                raise DatabaseError(f'Locus {locus.name} already exists in database {self.name}.')\n            self.loci[locus.name] = locus\n            for gene in locus:\n                if gene.name in self.genes:\n                    raise DatabaseError(f'Gene {gene} already exists in database {self.name}.')\n                self.genes[gene.name] = gene\n        else:\n            for gene in locus:\n                if gene.name in self.extra_genes:\n                    raise DatabaseError(f'Gene {gene} already exists in database {self.name}.')\n                self.extra_genes[gene.name] = gene\n\n    def add_phenotype(self, loci: list[str], genes: dict[str, str], phenotype: str):\n        extra_genes = {(g.name, 'present') for g in self.extra_genes.values() if g.gene_name in genes}\n        for locus in (self.loci.keys() if loci == [\"ALL\"] else loci):\n            if locus in self.loci:\n                if extra_genes:\n                    self.loci[locus].add_phenotype(None, extra_genes, phenotype)\n                else:\n                    self.loci[locus].add_phenotype(genes, None, phenotype, strict=loci != [\"ALL\"])\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.Database.add_locus","title":"<code>add_locus(locus)</code>","text":"<p>Adds a locus and its genes to the database. Checks that the locus and genes don't already exist in the database.</p> Source code in <code>kaptive/database.py</code> <pre><code>def add_locus(self, locus: Locus):\n    \"\"\"\n    Adds a locus and its genes to the database. Checks that the locus and genes don't already exist in the database.\n    \"\"\"\n    if not locus.name.startswith('Extra_genes'):\n        if locus.name in self.loci:\n            raise DatabaseError(f'Locus {locus.name} already exists in database {self.name}.')\n        self.loci[locus.name] = locus\n        for gene in locus:\n            if gene.name in self.genes:\n                raise DatabaseError(f'Gene {gene} already exists in database {self.name}.')\n            self.genes[gene.name] = gene\n    else:\n        for gene in locus:\n            if gene.name in self.extra_genes:\n                raise DatabaseError(f'Gene {gene} already exists in database {self.name}.')\n            self.extra_genes[gene.name] = gene\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.Gene","title":"<code>Gene</code>","text":"<p>This class prepares and stores a CDS feature from a Kaptive reference genbank file. It is designed so that the Feature itself doesn't need to be stored, only the information required to extract it from the record.</p> Source code in <code>kaptive/database.py</code> <pre><code>class Gene:\n    \"\"\"\n    This class prepares and stores a CDS feature from a Kaptive reference genbank file.\n    It is designed so that the Feature itself doesn't need to be stored, only the information required to\n    extract it from the record.\n    \"\"\"\n\n    def __init__(self, name: str, start: int = 0, end: int = 0, strand: str = \"+\",\n                 protein_seq: Seq = None, dna_seq: Seq = None, gene_name: str = None, product: str = None):\n        self.name = name or ''\n        self.start = start  # 0-based\n        self.end = end\n        self.strand = strand  # Either + or -\n        self.gene_name = gene_name or ''\n        self.product = product or ''  # Can also be description\n        self.dna_seq = dna_seq or Seq('')\n        self.protein_seq = protein_seq or Seq('')\n\n    def __hash__(self):\n        return hash(self.name)  # The name of the gene is unique and immutable\n\n    def __repr__(self):\n        return self.name\n\n    def __len__(self):\n        return len(self.dna_seq)\n\n    def format(self, format_spec):\n        if format_spec == 'ffn':\n            if len(self.dna_seq) == 0:\n                warning(f'No DNA sequence for {self}')\n                return \"\"\n            return f'&gt;{self.name}\\n{self.dna_seq}\\n'\n        if format_spec == 'faa':\n            self.extract_translation()\n            if len(self.protein_seq) == 0:\n                warning(f'No protein sequence for {self.__repr__()}')\n                return \"\"\n            return f'&gt;{self.name}\\n{self.protein_seq}\\n'\n        raise ValueError(f'Invalid format specifier: {format_spec}')\n\n    def extract_translation(self, **kwargs):\n        \"\"\"\n        Extracts the protein sequence from the DNA sequence of the gene. Implemented as a method so unnecessary\n        translations are not performed.\n        :param table: NCBI translation table number\n        :param cds: if True, only translates the CDS\n        :param to_stop: if True, stops translation at the first stop codon\n        :param gap: gap character\n        :param stop_symbol: stop codon character\n        \"\"\"\n        if len(self.protein_seq) == 0:  # Only translate if the protein sequence is not already stored\n            if len(self.dna_seq) == 0:\n                raise GeneError(f'No DNA sequence for reference {self}')\n            with catch_warnings(record=True) as w:\n                self.protein_seq = self.dna_seq.translate(**kwargs)\n                # for i in w:\n                #     warning(f\"{i.message}: {self.__repr__()}\")\n            if len(self.protein_seq) == 0:\n                warning(f'No protein sequence for reference {self}')\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.Gene.extract_translation","title":"<code>extract_translation(**kwargs)</code>","text":"<p>Extracts the protein sequence from the DNA sequence of the gene. Implemented as a method so unnecessary translations are not performed. :param table: NCBI translation table number :param cds: if True, only translates the CDS :param to_stop: if True, stops translation at the first stop codon :param gap: gap character :param stop_symbol: stop codon character</p> Source code in <code>kaptive/database.py</code> <pre><code>def extract_translation(self, **kwargs):\n    \"\"\"\n    Extracts the protein sequence from the DNA sequence of the gene. Implemented as a method so unnecessary\n    translations are not performed.\n    :param table: NCBI translation table number\n    :param cds: if True, only translates the CDS\n    :param to_stop: if True, stops translation at the first stop codon\n    :param gap: gap character\n    :param stop_symbol: stop codon character\n    \"\"\"\n    if len(self.protein_seq) == 0:  # Only translate if the protein sequence is not already stored\n        if len(self.dna_seq) == 0:\n            raise GeneError(f'No DNA sequence for reference {self}')\n        with catch_warnings(record=True) as w:\n            self.protein_seq = self.dna_seq.translate(**kwargs)\n            # for i in w:\n            #     warning(f\"{i.message}: {self.__repr__()}\")\n        if len(self.protein_seq) == 0:\n            warning(f'No protein sequence for reference {self}')\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.Locus","title":"<code>Locus</code>","text":"Source code in <code>kaptive/database.py</code> <pre><code>class Locus:\n    def __init__(self, name: str = None, seq: Seq | None = Seq(''), genes: dict[str: Gene] = None,\n                 type_label: str = None, phenotypes: list[tuple[set[tuple[str, str]], str]] = None,\n                 index: int | None = 0):\n        self.name = name or ''\n        self.seq = seq\n        self._length = len(self.seq)\n        self.genes = genes or {}\n        self.type_label = type_label or ''\n        self.phenotypes = phenotypes or []\n        self.index = index\n\n    @classmethod\n    def from_seqrecord(cls, record: SeqRecord, locus_name: str, type_name: str, load_seq: bool = True,\n                       extract_translations: bool = False):\n        if load_seq:\n            self = cls(name=locus_name, seq=record.seq)\n        else:\n            self = cls(name=locus_name)\n            self._length = len(record.seq)  # We are not loading the sequence, so we need to set the length manually\n        n = 1\n        for feature in record.features:  # type: SeqFeature\n            if feature.type == 'CDS':\n\n                name = f\"{locus_name}_{str(n).zfill(2)}\" + (\n                    f\"_{gene_name}\" if (gene_name := feature.qualifiers.get('gene', [''])[0]) else '')\n\n                gene = Gene(\n                    name=name, gene_name=gene_name, dna_seq=feature.extract(record.seq), start=feature.location.start,\n                    end=feature.location.end, strand='+' if feature.location.strand == 1 else '-',\n                    product=feature.qualifiers.get('product', [''])[0]\n                )\n\n                if not len(gene.dna_seq) % 3 == 0:  # Check the gene is a multiple of 3 (complete CDS)\n                    # TODO: this is quite strict, but enforces the inclusion of complete CDS in Kaptive databases\n                    raise GeneError(f'DNA sequence of {name} is not a multiple of 3')\n\n                if name in self.genes:\n                    raise LocusError(f'Gene {name} already exists in locus {self}')\n\n                if extract_translations:  # Force translation of the gene\n                    gene.extract_translation()\n\n                self.genes[name] = gene\n                n += 1\n        self.type_label = type_name if not self.name.startswith('Extra_genes') else None  # Extra genes don't have a type\n        return self\n\n    def __hash__(self):  # TODO: Check if this is used at all\n        return hash(self.name)  # The name of the locus is unique and immutable\n\n    def __repr__(self):\n        return self.name\n\n    def __len__(self):\n        return self._length or len(self.seq)\n\n    def __getitem__(self, item) -&gt; Gene:\n        if not (result := self.genes.get(item)):\n            raise LocusError(f'Could not find {item} in locus {self.name}')\n        return result\n\n    def __iter__(self):\n        return iter(self.genes.values())\n\n    def add_phenotype(self, genes: dict[str, str] | None, extra_genes: set[tuple[str, str]] | None, phenotype: str,\n                      strict: bool = False):\n        if extra_genes:\n            self.phenotypes = sorted(self.phenotypes + [(extra_genes, phenotype)], key=lambda x: len(x[0]),\n                                     reverse=True)\n        else:  # Turn gene names into a set of tuples with the gene name and state\n            genes = {(g.name, state) for g in self if\n                     (state := genes.get('ALL', genes.get(g.gene_name, genes.get(g.name, None))))}\n            if genes:\n                self.phenotypes = sorted(self.phenotypes + [(genes, phenotype)], key=lambda x: len(x[0]), reverse=True)\n            elif strict:  # If strict, raise an error\n                raise PhenotypeError(f\"Phenotype ({phenotype}) based on ({genes}) does not apply to {self}\")\n\n    def format(self, format_spec):\n        if format_spec == 'fna':\n            if len(self.seq) == 0:\n                warning(f'No DNA sequence for {self}')\n                return \"\"\n            return f'&gt;{self.name}\\n{self.seq}\\n'\n        if format_spec in {'ffn', 'faa'}:\n            return ''.join([gene.format(format_spec) for gene in self])\n        raise ValueError(f'Invalid format specifier: {format_spec}')\n\n    def write(self, fna: str | PathLike | TextIO = None, ffn: str | PathLike | TextIO = None,\n              faa: str | PathLike | TextIO = None):\n        \"\"\"Write the typing result to files or file handles.\"\"\"\n        for f, fmt in [(fna, 'fna'), (ffn, 'ffn'), (faa, 'faa')]:\n            if f:\n                if isinstance(f, TextIOBase):\n                    f.write(self.format(fmt))\n                elif isinstance(f, PathLike) or isinstance(f, str):\n                    with open(path.join(f, f'{self.name.replace(\"/\", \"_\")}.{fmt}'), 'wt') as handle:\n                        handle.write(self.format(fmt))\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.Locus.write","title":"<code>write(fna=None, ffn=None, faa=None)</code>","text":"<p>Write the typing result to files or file handles.</p> Source code in <code>kaptive/database.py</code> <pre><code>def write(self, fna: str | PathLike | TextIO = None, ffn: str | PathLike | TextIO = None,\n          faa: str | PathLike | TextIO = None):\n    \"\"\"Write the typing result to files or file handles.\"\"\"\n    for f, fmt in [(fna, 'fna'), (ffn, 'ffn'), (faa, 'faa')]:\n        if f:\n            if isinstance(f, TextIOBase):\n                f.write(self.format(fmt))\n            elif isinstance(f, PathLike) or isinstance(f, str):\n                with open(path.join(f, f'{self.name.replace(\"/\", \"_\")}.{fmt}'), 'wt') as handle:\n                    handle.write(self.format(fmt))\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.get_database","title":"<code>get_database(argument)</code>","text":"<p>Returns the path to the database file. If an existing file is passed, it is returned, otherwise it will be treated as a keyword and used to find the respective database in the kaptive package.</p> Source code in <code>kaptive/database.py</code> <pre><code>def get_database(argument: str | PathLike) -&gt; tuple[str, PathLike]:\n    \"\"\"\n    Returns the path to the database file.\n    If an existing file is passed, it is returned, otherwise it will be treated as a keyword and used to\n    find the respective database in the kaptive package.\n    \"\"\"\n    if path.isfile(argument):\n        return path.splitext(path.basename(argument))[0], check_file(argument)\n\n    if not (dbs_in_package := [i for i in listdir(_DB_PATH) if i.endswith('.gbk')]):\n        quit_with_error(f'No databases found in expected path: {_DB_PATH}')\n\n    # Check keywords\n    for db in dbs_in_package:\n        db_stem, _ = path.splitext(db)\n        if argument == db_stem or argument in _DB_KEYWORDS[db_stem]:\n            return db_stem, check_file(path.join(_DB_PATH, db))\n\n    quit_with_error(f'No database found for {argument}\\n'\n                    f'Available databases: {\", \".join(dbs_in_package)}\\n'\n                    f'Valid keywords: {\", \".join([i for x in _DB_KEYWORDS.values() for i in x])}')\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.name_from_record","title":"<code>name_from_record(record, locus_regex=_LOCUS_REGEX, type_regex=_TYPE_REGEX)</code>","text":"<p>This function extracts the locus and type names from a genbank record using regular expressions. If the locus_regex or type_regex are not provided, the default regexes are used.</p> Source code in <code>kaptive/database.py</code> <pre><code>def name_from_record(record: SeqRecord, locus_regex: re.Pattern | None = _LOCUS_REGEX,\n                     type_regex: re.Pattern | None = _TYPE_REGEX) -&gt; tuple[str | None, str | None]:\n    \"\"\"\n    This function extracts the locus and type names from a genbank record using regular expressions.\n    If the locus_regex or type_regex are not provided, the default regexes are used.\n    \"\"\"\n\n    locus_regex, type_regex = locus_regex or _LOCUS_REGEX, type_regex or _TYPE_REGEX  # If None, use the default regexes\n    locus_name, type_name = set(), set()\n\n    if not (source := next((f for f in record.features if f.type == 'source'), None)):\n        quit_with_error(f'Could not find source feature in genbank record: {record.id}')\n    if \"note\" not in source.qualifiers:\n        quit_with_error(f'Could not find note qualifier in source feature of genbank record: {record.id}')\n\n    for note in source.qualifiers['note']:\n        if type_regex and (match := type_regex.search(note)):\n            type_name.add(match.group())\n\n        if note.startswith('Extra genes'):  # \"Extra genes: gmlABD\" -&gt; \"Extra_genes_gmlABD\"\n            locus_name.add(f\"Extra_genes_{note.split(' ')[-1]}\")\n\n        elif locus_regex and (match := locus_regex.search(note)):\n            locus_name.add(match.group())\n\n    if len(locus_name) &gt; 1:\n        quit_with_error(f'Found multiple locus names in record: {record.id}\\n\\tNote: {source.qualifiers[\"note\"]}')\n    if len(type_name) &gt; 1:\n        quit_with_error(f'Found multiple type names in record: {record.id}\\n\\tNote: {source.qualifiers[\"note\"]}')\n\n    return locus_name.pop() if len(locus_name) == 1 else None, type_name.pop() if len(type_name) == 1 else None\n</code></pre>"},{"location":"reference/kaptive/database.html#kaptive.database.parse_database","title":"<code>parse_database(db, locus_filter=None, load_locus_seqs=True, extract_translations=False, verbose=False, **kwargs)</code>","text":"<p>Wrapper around SeqIO.parse to parse a Kaptive database genbank file and return a generator of Locus objects</p> Source code in <code>kaptive/database.py</code> <pre><code>def parse_database(db: str | PathLike, locus_filter: re.Pattern = None, load_locus_seqs: bool = True,\n                   extract_translations: bool = False, verbose: bool = False, **kwargs) -&gt; Generator[Locus, None, None]:\n    \"\"\"\n    Wrapper around SeqIO.parse to parse a Kaptive database genbank file and return a generator of Locus objects\n    \"\"\"\n    db_name, db_path = get_database(db)\n    log(f'Parsing {db_name}', verbose=verbose)\n    try:\n        for record in SeqIO.parse(db_path, 'genbank'):\n            locus_name, type_name = name_from_record(record, **kwargs)\n            if not locus_name:\n                quit_with_error(f'Could not parse locus name from {record.id}')\n            if type_name == \"unknown\" or (not type_name and not locus_name.startswith('Extra_genes')):\n                type_name = f'unknown ({locus_name})'  # Add the locus name to the type name if it is unknown\n            if locus_filter and not locus_filter.search(locus_name):\n                continue\n            yield Locus.from_seqrecord(record, locus_name, type_name, load_locus_seqs, extract_translations)\n    except Exception as e:\n        quit_with_error(f'Could not parse database {db_name}: {e}')\n</code></pre>"},{"location":"reference/kaptive/log.html","title":"log","text":""},{"location":"reference/kaptive/log.html#kaptive.log","title":"<code>kaptive.log</code>","text":"<p>This module contains functions for logging messages to stderr.</p> <p>Copyright 2023 Tom Stanton (tomdstanton@gmail.com) https://github.com/klebgenomics/Kaptive</p> <p>This file is part of Kaptive. Kaptive is free software: you can redistribute it and/or modify it under the terms of the GNU General Public License as published by the Free Software Foundation, either version 3 of the License, or (at your option) any later version. Kaptive is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License for more details. You should have received a copy of the GNU General Public License along with Kaptive. If not, see https://www.gnu.org/licenses/.</p>"},{"location":"reference/kaptive/log.html#kaptive.log.log","title":"<code>log(message='', verbose=True, rjust=20, stack_depth=1)</code>","text":"<p>Simple function for logging messages to stderr. Only runs if verbose == True. Stack depth can be increased if the parent function name needs to be exposed.</p> Source code in <code>kaptive/log.py</code> <pre><code>def log(message: str = '', verbose: bool = True, rjust: int = 20, stack_depth: int = 1):\n    \"\"\"\n    Simple function for logging messages to stderr. Only runs if verbose == True.\n    Stack depth can be increased if the parent function name needs to be exposed.\n    \"\"\"\n    if verbose:  # Only build log if verbosity is requested; simple way of controlling log\n        sys.stderr.write(f\"{datetime.now():%Y-%m-%d %H:%M:%S} {stack()[stack_depth].function:&gt;{rjust}}] {message}\\n\")\n</code></pre>"},{"location":"reference/kaptive/typing.html","title":"typing","text":""},{"location":"reference/kaptive/typing.html#kaptive.typing","title":"<code>kaptive.typing</code>","text":""},{"location":"reference/kaptive/typing.html#kaptive.typing.GeneResult","title":"<code>GeneResult</code>","text":"<p>Class to store alignment results for a single gene in a locus for either a ReadResult or a AssemblyResult.</p> Source code in <code>kaptive/typing.py</code> <pre><code>class GeneResult:\n    \"\"\"\n    Class to store alignment results for a single gene in a locus for either a ReadResult or a AssemblyResult.\n    \"\"\"\n\n    def __init__(self, id: str, gene: Gene, result: TypingResult = None,\n                 piece: LocusPiece = None, start: int | None = 0, end: int | None = 0, strand: str = None,\n                 dna_seq: Seq | None = Seq(\"\"), protein_seq: Seq | None = Seq(\"\"), below_threshold: bool | None = False,\n                 phenotype: str | None = \"present\", gene_type: str = None, partial: bool | None = False,\n                 percent_identity: float | None = 0, percent_coverage: float | None = 0):\n        self.id = id or ''  # TODO: rename as seq_id for clarity, actual id is self.__repr__()\n        self.gene = gene\n        self.result = result  # TODO: replace with sample_name (only TypingResult attribute used, needed for fa headers)\n        self.start = start\n        self.end = end\n        self.strand = strand\n        self.partial = partial\n        self.piece = piece  # inside locus if not None\n        self.dna_seq = dna_seq\n        self.protein_seq = protein_seq\n        self.below_threshold = below_threshold\n        self.phenotype = phenotype\n        self.gene_type = gene_type or \"\"\n        self.percent_identity = percent_identity\n        self.percent_coverage = percent_coverage\n\n    def __repr__(self):\n        return f\"{self.gene.name} {self.id}:{self.start}-{self.end}{self.strand}\"\n\n    def __len__(self):\n        return self.end - self.start\n\n    def __str__(self) -&gt; str:\n        s = f'{self.gene.name},{self.percent_identity:.2f}%,{self.percent_coverage:.2f}%'\n        s += \",partial\" if self.partial else \"\"\n        s += ',truncated' if self.phenotype == \"truncated\" else \"\"\n        s += \",below_id_threshold\" if self.below_threshold else \"\"\n        return s\n\n    @classmethod\n    def from_dict(cls, d: dict, **kwargs) -&gt; GeneResult:\n        return cls(\n            id=d['id'], start=int(d['start']), end=int(d['end']), strand=d['strand'], dna_seq=Seq(d['dna_seq']),\n            protein_seq=Seq(d['protein_seq']), below_threshold=True if d['below_threshold'] == 'True' else False,\n            phenotype=d['phenotype'], gene_type=d['gene_type'], partial=True if d['partial'] == 'True' else False,\n            percent_identity=float(d['percent_identity']), percent_coverage=float(d['percent_coverage']), **kwargs\n        )\n\n    def format(self, format_spec, relative_start: int = 0) -&gt; str | dict:\n        if format_spec == 'ffn':\n            if len(self.dna_seq) == 0:\n                warning(f'No DNA sequence for {self}')\n                return \"\"\n            return (f'&gt;{self.gene.name} {self.result.sample_name}|{self.id}:{self.start}-{self.end}{self.strand}\\n'\n                    f'{self.dna_seq}\\n')\n        if format_spec == 'faa':\n            if len(self.protein_seq) == 0:\n                warning(f'No protein sequence for {self.__repr__()}')\n                return \"\"\n            return (f'&gt;{self.gene.name} {self.result.sample_name}|{self.id}:{self.start}-{self.end}{self.strand}\\n'\n                    f'{self.protein_seq}\\n')\n        if format_spec == 'json':\n            return {\n                'id': self.id, 'start': str(self.start), 'end': str(self.end), 'strand': self.strand,\n                'dna_seq': str(self.dna_seq), 'protein_seq': str(self.protein_seq), 'partial': str(self.partial),\n                'below_threshold': str(self.below_threshold), 'phenotype': self.phenotype, 'gene_type': self.gene_type,\n                'percent_identity': str(self.percent_identity), 'percent_coverage': str(self.percent_coverage),\n                'gene': self.gene.name, 'piece': self.piece.__repr__() if self.piece else '',\n            }\n        raise ValueError(f\"Unknown format specifier {format_spec}\")\n\n    def compare_translation(self, truncation_tolerance: float = 95, **kwargs):\n        \"\"\"\n        Extracts the translation from the DNA sequence of the gene result.\n        Will also extract the translation from the gene if it is not already stored.\n        \"\"\"\n        self.gene.extract_translation(**kwargs)  # Extract the translation from the gene if it is not already stored\n        if len(self.dna_seq) == 0:  # If the DNA sequence is empty, raise an error\n            raise GeneResultError(f'No DNA sequence for {self.__repr__()}')\n        with catch_warnings(record=True) as w:  # Catch Biopython warnings\n            protein_seqs = [self.dna_seq[i:].translate(**kwargs) for i in range(3)]  # Translate in all 3 frames\n        frame, self.protein_seq = max(enumerate(protein_seqs), key=lambda x: len(x[1]))  # Get the longest translation\n        self.start += frame  # Update the start position to the frame with the longest translation\n        if len(self.protein_seq) &lt;= 1:  # If the protein sequence is still empty, raise a warning\n            warning(f'No protein sequence for {self.__repr__()}')\n        elif len(self.gene.protein_seq) &gt; 1:  # If both sequences are not empty\n            if alignments := _PROTEIN_ALIGNER.align(self.gene.protein_seq, self.protein_seq):  # Align the sequences\n                alignment = max(alignments, key=lambda x: x.score)  # Get the best alignment\n                self.percent_identity = alignment.counts().identities / alignment.length * 100\n                self.percent_coverage = (len(self.protein_seq) / len(self.gene.protein_seq)) * 100\n                if (self.percent_coverage &lt; truncation_tolerance and  # If the coverage is less than the tolerance\n                        # not partial, and not unexpected gene outside locus\n                        not self.partial and not (self.gene_type == \"unexpected_genes\" and not self.piece)):\n                    self.phenotype = \"truncated\"  # Set the phenotype to truncated\n            else:\n                warning(f'Error aligning {self.__repr__()}')\n</code></pre>"},{"location":"reference/kaptive/typing.html#kaptive.typing.GeneResult.compare_translation","title":"<code>compare_translation(truncation_tolerance=95, **kwargs)</code>","text":"<p>Extracts the translation from the DNA sequence of the gene result. Will also extract the translation from the gene if it is not already stored.</p> Source code in <code>kaptive/typing.py</code> <pre><code>def compare_translation(self, truncation_tolerance: float = 95, **kwargs):\n    \"\"\"\n    Extracts the translation from the DNA sequence of the gene result.\n    Will also extract the translation from the gene if it is not already stored.\n    \"\"\"\n    self.gene.extract_translation(**kwargs)  # Extract the translation from the gene if it is not already stored\n    if len(self.dna_seq) == 0:  # If the DNA sequence is empty, raise an error\n        raise GeneResultError(f'No DNA sequence for {self.__repr__()}')\n    with catch_warnings(record=True) as w:  # Catch Biopython warnings\n        protein_seqs = [self.dna_seq[i:].translate(**kwargs) for i in range(3)]  # Translate in all 3 frames\n    frame, self.protein_seq = max(enumerate(protein_seqs), key=lambda x: len(x[1]))  # Get the longest translation\n    self.start += frame  # Update the start position to the frame with the longest translation\n    if len(self.protein_seq) &lt;= 1:  # If the protein sequence is still empty, raise a warning\n        warning(f'No protein sequence for {self.__repr__()}')\n    elif len(self.gene.protein_seq) &gt; 1:  # If both sequences are not empty\n        if alignments := _PROTEIN_ALIGNER.align(self.gene.protein_seq, self.protein_seq):  # Align the sequences\n            alignment = max(alignments, key=lambda x: x.score)  # Get the best alignment\n            self.percent_identity = alignment.counts().identities / alignment.length * 100\n            self.percent_coverage = (len(self.protein_seq) / len(self.gene.protein_seq)) * 100\n            if (self.percent_coverage &lt; truncation_tolerance and  # If the coverage is less than the tolerance\n                    # not partial, and not unexpected gene outside locus\n                    not self.partial and not (self.gene_type == \"unexpected_genes\" and not self.piece)):\n                self.phenotype = \"truncated\"  # Set the phenotype to truncated\n        else:\n            warning(f'Error aligning {self.__repr__()}')\n</code></pre>"},{"location":"reference/kaptive/typing.html#kaptive.typing.TypingResult","title":"<code>TypingResult</code>","text":"<p>This is a class to store the results of a typing analysis for a single sample. It is designed to be flexible enough to store results from both read and assembly typing, and can be easily reconstructed from JSON to use with the <code>convert</code> utility. It should not store any information that is not directly related to the typing such as contig sequences or read alignments.</p> Source code in <code>kaptive/typing.py</code> <pre><code>class TypingResult:\n    \"\"\"\n    This is a class to store the results of a typing analysis for a single sample. It is designed to be flexible\n    enough to store results from both read and assembly typing, and can be easily reconstructed from JSON to use\n    with the `convert` utility. It should not store any information that is not directly related to the typing\n    such as contig sequences or read alignments.\n    \"\"\"\n\n    def __init__(\n            self, sample_name: str | None, db: Database | None, best_match: Locus = None,\n            pieces: list[LocusPiece] = None, expected_genes_inside_locus: list[GeneResult] = None,\n            expected_genes_outside_locus: list[GeneResult] = None, missing_genes: list[str] = None,\n            unexpected_genes_inside_locus: list[GeneResult] = None,\n            unexpected_genes_outside_locus: list[GeneResult] = None,\n            extra_genes: list[GeneResult] = None):\n        self.sample_name = sample_name or \"\"\n        self.db = db\n        self.best_match = best_match\n        self.pieces = pieces or []  # Pieces of locus reconstructed from alignments\n        self.expected_genes_inside_locus = expected_genes_inside_locus or []  # Genes from best_match\n        self.expected_genes_outside_locus = expected_genes_outside_locus or []  # Genes from best_match\n        self.missing_genes = missing_genes or []  # Genes from best_match that were not found\n        self.unexpected_genes_inside_locus = unexpected_genes_inside_locus or []  # Genes from other loci\n        self.unexpected_genes_outside_locus = unexpected_genes_outside_locus or []  # Genes from other loci\n        self.extra_genes = extra_genes or []  # in db.extra_genes, ALWAYS outside locus (gene_result.piece == None)\n        # Properties to cache the values, these are protected to prevent accidental modification\n        self._percent_identity = None\n        self._percent_coverage = None\n        self._phenotype = None\n        self._problems = None\n        self._confidence = None\n\n    def __repr__(self):\n        return f\"{self.sample_name} {self.best_match.name}\"\n\n    def __len__(self):\n        return sum(len(i) for i in self.pieces) if self.pieces else 0\n\n    def __iter__(self):\n        return chain(\n            self.expected_genes_inside_locus, self.unexpected_genes_inside_locus,\n            self.expected_genes_outside_locus, self.unexpected_genes_outside_locus, self.extra_genes)\n\n    def add_gene_result(self, gene_result: GeneResult):\n        if gene_result.piece:  # If gene_result.piece is not None, the gene is inside the locus\n            gene_result.piece.add_gene_result(gene_result)\n            gene_type = f\"{gene_result.gene_type}{'_inside_locus' if gene_result.gene_type.startswith(('expected', 'unexpected')) else ''}\"\n        else:  # If gene_result.piece is None, the gene is outside the locus\n            gene_type = f\"{gene_result.gene_type}{'_outside_locus' if gene_result.gene_type.startswith(('expected', 'unexpected')) else ''}\"\n        getattr(self, gene_type).append(gene_result)  # Add gene result to the appropriate list\n\n    @property\n    def percent_identity(self) -&gt; float:\n        if self._percent_identity is None:\n            self._percent_identity = (sum(i.percent_identity for i in x) / len(x)) if (\n                x := self.expected_genes_inside_locus) else 0\n        return self._percent_identity\n\n    @property\n    def percent_coverage(self) -&gt; float:\n        if self._percent_coverage is None:\n            self._percent_coverage = sum(len(i) for i in x) / sum(len(i) for i in self.best_match.genes.values()) * 100 \\\n                if (x := self.expected_genes_inside_locus) else 0\n        return self._percent_coverage\n\n    @property\n    def phenotype(self) -&gt; str:\n        if self._phenotype is None:\n            gene_phenotypes = set()  # Init set to store gene phenotypes to be used as a key in the phenotypes dict\n            for gene in self:\n                if gene.gene_type in {'expected_genes', 'extra_genes'}:  # The reported phenotype only considers these\n                    gene_phenotypes.add((gene.gene.name, gene.phenotype))  # or extra genes\n            # NOTE: The best_match.phenotypes MUST be sorted from largest to smallest gene set to make sure any sets with\n            # extra genes are tested first.\n            self._phenotype = next(\n                (p for gs, p in self.best_match.phenotypes if len(gs) == len(gene_phenotypes.intersection(gs))),\n                self.best_match.type_label)  # If no phenotype is found, return the type label\n        return self._phenotype\n\n    @property\n    def problems(self) -&gt; str:\n        if self._problems is None:\n            self._problems = f'?{x}' if (x := len(self.pieces)) != 1 else ''\n            self._problems += '-' if self.missing_genes else ''\n            self._problems += '+' if self.unexpected_genes_inside_locus else ''\n            self._problems += '*' if any(\n                i.percent_coverage &gt;= 90 and i.below_threshold for i in self.expected_genes_inside_locus) else ''\n            self._problems += '!' if any(i.phenotype == \"truncated\" for i in self) else ''\n        return self._problems\n\n    @property\n    def confidence(self) -&gt; str:\n        return self._confidence if self._confidence is not None else \"Not calculated\"\n\n    def get_confidence(self, allow_below_threshold: bool, max_other_genes: int, percent_expected_genes: float):\n        p = len(set(i.gene.name for i in self.expected_genes_inside_locus)) / len(self.best_match.genes) * 100\n        other_genes = len(\n            set(i.gene.name for i in self.unexpected_genes_inside_locus if not i.phenotype == \"truncated\"))\n        if not allow_below_threshold and \"*\" in self.problems:\n            self._confidence = \"Untypeable\"\n        else:\n            if len(self.pieces) == 1 and not self.missing_genes and not other_genes:\n                self._confidence = \"Typeable\"\n            elif other_genes &lt;= max_other_genes and p &gt;= percent_expected_genes:\n                self._confidence = \"Typeable\"\n            else:\n                self._confidence = \"Untypeable\"\n\n    @classmethod\n    def from_dict(cls, d: dict, db: Database) -&gt; TypingResult:\n        if not (best_match := db.loci.get(d['best_match'])):\n            raise TypingResultError(f\"Best match {d['best_match']} not found in database\")\n        self = TypingResult(sample_name=d['sample_name'], db=db, best_match=best_match,\n                            missing_genes=d['missing_genes'])\n        # Set the cached properties\n        self._percent_identity = float(d['percent_identity'])\n        self._percent_coverage = float(d['percent_coverage'])\n        self._phenotype = d['phenotype']\n        self._problems = d['problems']\n        self._confidence = d['confidence']\n        # Add the pieces and create the gene results\n        self.pieces = [LocusPiece.from_dict(i, result=self) for i in d['pieces']]\n        pieces = {i.__repr__(): i for i in self.pieces}\n        gene_results = {}  # This was previously a dict comp, but we need to check the gene is in the database, see #31\n        for r in chain(d['expected_genes_inside_locus'], d['unexpected_genes_inside_locus'],\n                       d['expected_genes_outside_locus'], d['unexpected_genes_outside_locus'],\n                       d['extra_genes']):\n            if not (gene := db.genes.get(r['gene'])) and not (gene := db.extra_genes.get(r['gene'])):\n                raise TypingResultError(f\"Gene {r['gene']} not found in database\")\n            x = GeneResult.from_dict(r, result=self, piece=pieces.get(r['piece']), gene=gene)\n            gene_results[x.__repr__()] = x\n\n        for gene_result in gene_results.values():\n            self.add_gene_result(gene_result)\n        return self\n\n    def format(self, format_spec) -&gt; str | dict:\n        if format_spec == 'tsv':\n            return '\\t'.join(\n                [\n                    self.sample_name,\n                    self.best_match.name,\n                    self.phenotype,\n                    self.confidence,\n                    self.problems,\n                    f\"{self.percent_identity:.2f}%\",\n                    f\"{self.percent_coverage:.2f}%\",\n                    f\"{self.__len__() - len(self.best_match)} bp\" if len(self.pieces) == 1 else 'n/a',\n                    f\"{(n_inside := len({i.gene.name for i in self.expected_genes_inside_locus}))} / {(n_expected := len(self.best_match.genes))} ({100 * n_inside / n_expected:.2f}%)\",\n                    ';'.join(map(str, self.expected_genes_inside_locus)),\n                    ';'.join(self.missing_genes),\n                    f\"{len(self.unexpected_genes_inside_locus)}\",\n                    ';'.join(map(str, self.unexpected_genes_inside_locus)),\n                    f\"{(n_outside := len({i.gene.name for i in self.expected_genes_outside_locus}))} / {n_expected} ({100 * n_outside / n_expected:.2f}%)\",\n                    ';'.join(map(str, self.expected_genes_outside_locus)),\n                    f\"{len(self.unexpected_genes_outside_locus)}\",\n                    ';'.join(map(str, self.unexpected_genes_outside_locus)),\n                    ';'.join(map(str, filter(lambda z: z.phenotype == \"truncated\", self))),\n                    ';'.join(map(str, self.extra_genes))\n                ]\n            ) + \"\\n\"\n        if format_spec == 'fna':  # Return the nucleotide sequence of the locus\n            return \"\".join([i.format(format_spec) for i in self.pieces])\n        if format_spec in {'faa', 'ffn'}:  # Return the protein or nucleotide sequence of gene results\n            return \"\".join([i.format(format_spec) for i in self])\n        if format_spec == 'json':\n            return dumps(\n                {\n                    'sample_name': self.sample_name, 'best_match': self.best_match.name, 'confidence': self.confidence,\n                    'phenotype': self.phenotype, 'problems': self.problems,\n                    'percent_identity': str(self.percent_identity),\n                    'percent_coverage': str(self.percent_coverage), 'missing_genes': self.missing_genes\n                } | {\n                    attr: [i.format(format_spec) for i in getattr(self, attr)] for attr in {\n                        'pieces', 'expected_genes_inside_locus', 'unexpected_genes_inside_locus',\n                        'expected_genes_outside_locus', 'unexpected_genes_outside_locus', 'extra_genes'\n                    }\n                }) + \"\\n\"\n        raise ValueError(f\"Unknown format specifier {format_spec}\")\n\n    def write(self,\n              tsv: TextIO = None,\n              json: TextIO = None,\n              fna: str | PathLike | TextIO = None,\n              ffn: str | PathLike | TextIO = None,\n              faa: str | PathLike | TextIO = None,\n              ):\n        \"\"\"Write the typing result to files or file handles.\"\"\"\n        [f.write(self.format(fmt)) for f, fmt in [(tsv, 'tsv'), (json, 'json')] if isinstance(f, TextIOBase)]\n        for f, fmt in [(fna, 'fna'), (ffn, 'ffn'), (faa, 'faa')]:\n            if f:\n                if isinstance(f, TextIOBase):\n                    f.write(self.format(fmt))\n                elif isinstance(f, PathLike) or isinstance(f, str):\n                    with open(path.join(f, f'{self.sample_name}_kaptive_results.{fmt}'), 'wt') as handle:\n                        handle.write(self.format(fmt))\n</code></pre>"},{"location":"reference/kaptive/typing.html#kaptive.typing.TypingResult.write","title":"<code>write(tsv=None, json=None, fna=None, ffn=None, faa=None)</code>","text":"<p>Write the typing result to files or file handles.</p> Source code in <code>kaptive/typing.py</code> <pre><code>def write(self,\n          tsv: TextIO = None,\n          json: TextIO = None,\n          fna: str | PathLike | TextIO = None,\n          ffn: str | PathLike | TextIO = None,\n          faa: str | PathLike | TextIO = None,\n          ):\n    \"\"\"Write the typing result to files or file handles.\"\"\"\n    [f.write(self.format(fmt)) for f, fmt in [(tsv, 'tsv'), (json, 'json')] if isinstance(f, TextIOBase)]\n    for f, fmt in [(fna, 'fna'), (ffn, 'ffn'), (faa, 'faa')]:\n        if f:\n            if isinstance(f, TextIOBase):\n                f.write(self.format(fmt))\n            elif isinstance(f, PathLike) or isinstance(f, str):\n                with open(path.join(f, f'{self.sample_name}_kaptive_results.{fmt}'), 'wt') as handle:\n                    handle.write(self.format(fmt))\n</code></pre>"},{"location":"reference/kaptive/utils.html","title":"utils","text":""},{"location":"reference/kaptive/utils.html#kaptive.utils","title":"<code>kaptive.utils</code>","text":""},{"location":"reference/kaptive/utils.html#kaptive.utils.check_file","title":"<code>check_file(file, panic=False)</code>","text":"<p>Checks a file exists and is non-empty and returns the absolute path</p> Source code in <code>kaptive/utils.py</code> <pre><code>def check_file(file: str | os.PathLike, panic: bool = False) -&gt; os.PathLike | None:\n    \"\"\"Checks a file exists and is non-empty and returns the absolute path\"\"\"\n    func = quit_with_error if panic else warning\n    if not os.path.exists(file):\n        return func(f'{file} does not exist')\n    if not os.path.isfile(file):\n        return func(f'{file} is not a file')\n    elif not os.path.getsize(file):\n        return func(f'{file} is empty')\n    else:\n        return os.path.abspath(file)\n</code></pre>"},{"location":"reference/kaptive/utils.html#kaptive.utils.check_out","title":"<code>check_out(path, mode='at', exist_ok=True)</code>","text":"<p>Check if the user wants to create/append a file or directory. If it looks like/is already a file (has an extension), return the file object. If it looks like/is already a directory, return the directory path.</p> Source code in <code>kaptive/utils.py</code> <pre><code>def check_out(path: str | os.PathLike, mode: str = \"at\", exist_ok: bool = True) -&gt; os.PathLike | TextIO:\n    \"\"\"\n    Check if the user wants to create/append a file or directory.\n    If it looks like/is already a file (has an extension), return the file object.\n    If it looks like/is already a directory, return the directory path.\n    \"\"\"\n    if path == '-':  # If the path is '-', return stdout\n        return sys.stdout\n    if os.path.splitext(path)[1]:  # If the path has an extension, it's probably a file\n        try:\n            return open(path, mode)  # Open the file\n        except Exception as e:\n            quit_with_error(f'Could not open {path}: {e}')\n    if not os.path.exists(path):  # Assume directory\n        try:\n            os.makedirs(path, exist_ok=exist_ok)  # Create the directory if it doesn't exist\n        except Exception as e:\n            quit_with_error(f'Could not create {path}: {e}')\n    return path\n</code></pre>"},{"location":"reference/kaptive/utils.html#kaptive.utils.check_programs","title":"<code>check_programs(progs, verbose=False)</code>","text":"<p>Check if programs are installed and executable</p> Source code in <code>kaptive/utils.py</code> <pre><code>def check_programs(progs: list[str], verbose: bool = False):\n    \"\"\"Check if programs are installed and executable\"\"\"\n    bins = {  # Adapted from: https://unix.stackexchange.com/a/261971/375975\n        binary: x for path in filter(\n            os.path.isdir, os.environ[\"PATH\"].split(os.path.pathsep)\n        ) for binary in os.listdir(path) if os.access((x := os.path.join(path, binary)), os.X_OK)\n    }\n    for program in progs:\n        if program in bins:\n            log(f'{program}: {bins[program]}', verbose=verbose)\n        else:\n            quit_with_error(f'{program} not found')\n</code></pre>"},{"location":"reference/kaptive/utils.html#kaptive.utils.merge_ranges","title":"<code>merge_ranges(ranges, tolerance=0, skip_sort=False)</code>","text":"<p>Merge overlapping ranges :param ranges: List of tuples of start and end positions :param tolerance: Integer or float of tolerance for merging ranges :param skip_sort: Skip sorting the ranges before merging :return: List of merged ranges</p> Source code in <code>kaptive/utils.py</code> <pre><code>def merge_ranges(ranges: list[tuple[int | float, int | float]], tolerance: int | float = 0, skip_sort: bool = False\n                 ) -&gt; Generator[tuple[int | float, int | float], None, None]:\n    \"\"\"\n    Merge overlapping ranges\n    :param ranges: List of tuples of start and end positions\n    :param tolerance: Integer or float of tolerance for merging ranges\n    :param skip_sort: Skip sorting the ranges before merging\n    :return: List of merged ranges\n    \"\"\"\n    if not ranges:\n        return None\n    if len(ranges) == 1:\n        yield ranges[0]\n        return None\n    current_range = (ranges := ranges if skip_sort else sorted(ranges, key=itemgetter(0)))[0]  # Start with the first range\n    for start, end in ranges[1:]:  # Iterate through the ranges\n        if start - tolerance &lt;= current_range[1]:  # Overlap, merge the ranges\n            current_range = (current_range[0], max(current_range[1], end))\n        else:  # No overlap, add the current range to the merged list and start a new range\n            yield current_range  # Yield the current range\n            current_range = (start, end)   # Start a new range\n    yield current_range  # Yield the last range\n</code></pre>"},{"location":"reference/kaptive/utils.html#kaptive.utils.opener","title":"<code>opener(file, verbose=False, *args, **kwargs)</code>","text":"<p>Opens a file with the appropriate open function based on the magic bytes at the beginning of the data :param file: File to open :param verbose: Print log messages to stderr :return: File handle</p> Source code in <code>kaptive/utils.py</code> <pre><code>def opener(file: str | os.PathLike, verbose: bool = False, *args, **kwargs) -&gt; TextIO | BinaryIO:\n    \"\"\"\n    Opens a file with the appropriate open function based on the magic bytes at the beginning of the data\n    :param file: File to open\n    :param verbose: Print log messages to stderr\n    :return: File handle\n    \"\"\"\n    try:\n        file = check_file(file)\n    except FileNotFoundError as e:\n        raise e\n    basename = os.path.basename(file)\n    with open(file, 'rb') as f:  # Open the file to read bytes\n        first_bytes = f.read(_MIN_N_BYTES)  # Get the bytes necessary to guess the compression type\n    for magic, compression in _MAGIC_BYTES.items():\n        if first_bytes.startswith(magic):\n            log(f\"Assuming {basename} is compressed with {compression}\", verbose=verbose)\n            try:\n                return _OPEN[compression](file, *args, **kwargs)\n            except Exception as e:\n                return warning(f\"Error opening {basename} with {compression}; {first_bytes=}\\n{e}\")\n    log(f\"Assuming {basename} is uncompressed\", verbose=verbose)\n    return open(file, *args, **kwargs)\n</code></pre>"},{"location":"reference/kaptive/utils.html#kaptive.utils.range_overlap","title":"<code>range_overlap(range1, range2, skip_sort=False)</code>","text":"<p>Returns the overlap between two ranges :param range1: Tuple of start and end positions :param range2: Tuple of start and end positions :param skip_sort: Skip sorting each range before calculating the overlap :return: Integer of overlap</p> Source code in <code>kaptive/utils.py</code> <pre><code>def range_overlap(range1: tuple[int, int], range2: tuple[int, int], skip_sort: bool = False) -&gt; int:\n    \"\"\"\n    Returns the overlap between two ranges\n    :param range1: Tuple of start and end positions\n    :param range2: Tuple of start and end positions\n    :param skip_sort: Skip sorting each range before calculating the overlap\n    :return: Integer of overlap\n    \"\"\"\n    start1, end1 = range1 if skip_sort else sorted(range1)\n    start2, end2 = range2 if skip_sort else sorted(range2)\n    overlap_start = max(start1, start2)\n    overlap_end = min(end1, end2)\n    return max(0, overlap_end - overlap_start)\n</code></pre>"}]}